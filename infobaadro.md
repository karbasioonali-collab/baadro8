# مستندات پروژه بادرو (Badro)

> این فایل مرجع داخلی وضعیت پروژه است — بعد از هر تغییر مهم باید به‌روزرسانی شود (بخش «تاریخچه‌ی تغییرات» در پایین).
> **هیچ رمز عبور یا API Key واقعی در این فایل نوشته نمی‌شود** — فقط اسم متغیرهای محیطی و نمونه‌رمزهای تست ذکر می‌شود.
> این نسخه یک بازنویسی کامل است (۲۰۲۶-۰۷-۳۰) تا هر مهندس یا نشست جدید Claude Code بدون نیاز به خواندن کل تاریخچه‌ی مکالمات قبلی، بتواند سریع context بگیرد.

---

## ⚠️ نکات حیاتی — قبل از هر کاری این بخش را کامل بخوانید

### ۱. `scripts/start.sh` روی دیپلوی فعلی هیچ‌وقت اجرا نمی‌شود

محیط دیپلوی فعلی **Liara** است، با `output: standalone` در `next.config.ts`. پلتفرم برای اپ‌های Next.js با خروجی standalone، مستقیماً فایل `server.js` تولیدشده را اجرا می‌کند — **نه** دستور `"start": "sh scripts/start.sh"` که در `package.json` تعریف شده را.

پیامد این موضوع: دو مرحله‌ای که قرار بود طبق `scripts/start.sh` **خودکار و قبل از بالا آمدن هر بار سرور** اجرا شوند، هیچ‌وقت اجرا نمی‌شوند:
- `prisma migrate deploy` (اعمال migration های ساختاری روی دیتابیس)
- `prisma db seed` (ساخت داده اولیه/حساب‌های پیش‌فرض)

**نتیجه‌ی عملی:** هر تغییری که نیاز به migration دیتابیس یا seed داشته باشد، تا وقتی این محدودیت پلتفرم رفع نشود (یا یک Dockerfile اختصاصی اضافه شود، یا Liara یک «Release Command» رسمی ارائه بدهد)، باید **دستی** از طریق Shell سرور Liara اجرا شود. راهنمای گام‌به‌گام این کار در بخش [۳. راهنمای اجرای migration دستی](#۳-راهنمای-اجرای-migration-دستی) آمده.

نکته‌ی مهم‌تر: چون این محیط `standalone` است، خودِ **Prisma CLI هم روی سرور production نصب نیست** (فقط فایل‌های لازم برای اجرای خودِ سرور Next.js کپی می‌شوند، نه ابزارهای توسعه). یعنی حتی اگر به Shell وصل شوید، `npx prisma migrate deploy` هم کار نمی‌کند. برای تغییرات ساختاری باید یک اسکریپت Node مستقل نوشت که با پکیج `pg` مستقیم SQL اجرا کند.

این محدودیت **فقط** روی migration/seed اثر دارد. کاربردهای عادی سایت (ثبت مشتری با OTP، ثبت سفارش، ساخت شرکت/کارمند جدید از پنل ادمین، تعریف فرمول قیمت، تغییر وضعیت سفارش و…) هرکدام فقط `INSERT`/`UPDATE` معمولی روی جدول‌های موجودند (نه تغییر ساختار)، از طریق Server Action اجرا می‌شوند، و کاملاً خودکار و بدون هیچ مداخله‌ی دستی کار می‌کنند.

### ۲. حادثه‌ی migration دستی وزن (`weightKg` → `weightGrams`) — نیاز به تایید فوری

وقتی فیلد وزن سفارش از کیلوگرم به گرم تغییر کرد (بخش تاریخچه، ۲۰۲۶-۰۷-۳۰)، یک فایل migration رسمی در `prisma/migrations/20260730150457_convert_order_weight_to_grams/migration.sql` ساخته شد با این محتوا:

```sql
ALTER TABLE "orders"
  ALTER COLUMN "weightKg" TYPE INTEGER USING ROUND("weightKg" * 1000)::INTEGER;
ALTER TABLE "orders" RENAME COLUMN "weightKg" TO "weightGrams";
```

اما چون طبق بند ۱ بالا `scripts/start.sh` هیچ‌وقت روی سرور واقعی اجرا نشد، این migration **هیچ‌وقت از طریق `prisma migrate deploy` روی دیتابیس production اعمال نشد**. کاربر (نه دستیار) مستقیماً از طریق Shell سرور Liara و یک اسکریپت Node با پکیج `pg`، این دو دستور SQL را دستی روی دیتابیس واقعی اجرا کرد:

```sql
ALTER TABLE orders RENAME COLUMN "weightKg" TO "weightGrams";
UPDATE orders SET "weightGrams" = "weightGrams" * 1000;
```

**دو مشکل واقعی از این مغایرت به‌وجود آمده که باید تایید/رفع شوند:**

#### الف) عدم تطابق بین SQL دستی و migration رسمی — ریسک نوع ستون (Type)

migration رسمی، ستون را از `DECIMAL(6,2)` به `INTEGER` **تغییر نوع** هم می‌دهد (`ALTER COLUMN ... TYPE INTEGER`). SQL دستی‌ای که واقعاً اجرا شده **این تغییر نوع را ندارد** — فقط تغییر نام ستون و ضرب مقادیر در ۱۰۰۰ انجام شده. یعنی به احتمال زیاد **ستون `weightGrams` روی دیتابیس واقعی هنوز از نوع `DECIMAL(6,2)` است، نه `INTEGER`**، درحالی‌که `schema.prisma` می‌گوید `weightGrams Int?`.

**باید فوراً روی دیتابیس production تایید شود:**
```sql
SELECT column_name, data_type, numeric_precision, numeric_scale
FROM information_schema.columns
WHERE table_name = 'orders' AND column_name = 'weightGrams';
```
اگر `data_type` چیزی غیر از `integer` بود (مثلاً `numeric`)، باید یک ALTER اصلاحی دیگر اجرا شود:
```sql
ALTER TABLE orders ALTER COLUMN "weightGrams" TYPE INTEGER USING ROUND("weightGrams")::INTEGER;
```
(دقت: اینجا دیگر نباید دوباره در ۱۰۰۰ ضرب شود — مقادیر از قبل به گرم تبدیل شده‌اند، فقط باید نوع واقعی ستون هم با schema هماهنگ شود.)

#### ب) ریسک واقعی سرریز عددی (numeric overflow) حین اجرای `UPDATE`

`DECIMAL(6,2)` یعنی حداکثر ۶ رقم معنادار، ۲ رقم بعد از اعشار — یعنی بیشترین مقدار قابل‌ذخیره `9999.99` است. هر سفارشی که وزن اصلی‌اش (بر حسب کیلوگرم، قبل از تبدیل) **۱۰ کیلوگرم یا بیشتر** بوده، بعد از ضرب در ۱۰۰۰ به عددی مثل `10000.00` یا بزرگ‌تر می‌رسیده که **از ظرفیت `DECIMAL(6,2)` سرریز می‌کند** و PostgreSQL برای آن خطای `numeric field overflow` می‌دهد. چون `UPDATE` یک دستور SQL واحد است، اگر حتی یک ردیف چنین خطایی بدهد، **کل دستور UPDATE به‌صورت اتمیک rollback می‌شود** — یعنی اگر در آن لحظه حتی یک سفارش با وزن ≥۱۰ کیلوگرم در جدول وجود داشته (و seed خودِ پروژه تعرفه‌هایی تا ۵۰ کیلوگرم دارد، پس کاملاً محتمل است)، **کل `UPDATE` ممکن است اصلاً اجرا نشده باشد** درحالی‌که `RENAME COLUMN` (که یک دستور DDL جداگانه است) احتمالاً موفق بوده.

**باید فوراً روی دیتابیس production تایید شود:**
```sql
SELECT "trackingCode", "weightGrams" FROM orders WHERE "weightGrams" IS NOT NULL ORDER BY "weightGrams" DESC LIMIT 20;
```
مقادیر را چک کنید: آیا همه منطقی به‌نظر می‌رسند (چند صد تا چند ده‌هزار گرم)، یا مقادیری هست که بی‌معنی/خیلی کوچک/آسیب‌دیده به‌نظر می‌رسد (نشانه‌ی این‌که تبدیل درست انجام نشده)؟

#### ج) هماهنگ‌سازی تاریخچه‌ی migration با Prisma

چون این تغییر با SQL دستی انجام شده نه با `prisma migrate deploy`، جدول داخلی `_prisma_migrations` روی دیتابیس production **هیچ ردیفی برای `20260730150457_convert_order_weight_to_grams` ندارد**. اگر یک روز `prisma migrate deploy` روی این دیتابیس اجرا شود (چه به‌صورت خودکار بعد از رفع مشکل بند ۱، چه دستی)، Prisma این migration را «هنوز اجرا نشده» تشخیص می‌دهد و تلاش می‌کند دوباره اجرایش کند — که چون ستون `weightKg` دیگر وجود ندارد (به `weightGrams` تغییر نام داده)، با خطا (`column "weightKg" does not exist`) شکست می‌خورد و **کل فرآیند `migrate deploy` را متوقف می‌کند** (چون migration ها به‌ترتیب اجرا می‌شوند و با اولین خطا متوقف می‌شوند) — یعنی هیچ migration بعدی هم اعمال نمی‌شود.

**راه‌حل رسمی Prisma برای دقیقاً همین وضعیت** (تغییر دستی دیتابیس هماهنگ با یک migration موجود، بدون ثبت در تاریخچه) دستور `migrate resolve` است، **نه** ساخت یک migration جدید/خالی:
```sh
npx prisma migrate resolve --applied 20260730150457_convert_order_weight_to_grams
```
این دستور باید از محیطی اجرا شود که هم **Prisma CLI کامل** روی آن نصب باشد (نه سرور production standalone) و هم `DATABASE_URL` به همان دیتابیس production اشاره کند — مثلاً یک چک‌اوت کامل پروژه روی لپ‌تاپ توسعه‌دهنده با `DATABASE_URL` موقتاً ست‌شده روی مقدار production (با احتیاط کامل، و پاک‌کردن آن env var بلافاصله بعد از اجرا).

**ترتیب پیشنهادی برای رفع کامل این حادثه:**
1. تایید نوع واقعی ستون (بند الف) — اگر لازم بود، اصلاحش کنید.
2. بررسی صحت مقادیر (بند ب) — اگر داده خراب/ناقص بود، با یک اسکریپت اصلاحی دیگر ترمیمش کنید.
3. بعد از اطمینان از این‌که وضعیت واقعی دیتابیس دقیقاً با چیزی که migration رسمی توصیف می‌کند یکی است، `prisma migrate resolve --applied ...` را اجرا کنید تا تاریخچه هماهنگ شود.

### ۳. حساب‌های تست seed.ts — هیچ‌وقت واقعاً ساخته نشده‌اند

در `prisma/seed.ts` دو حساب تشخیصی idempotent تعریف شده:
- ادمین: `testadmin` / `Test@12345`
- شرکت: `testcompany` / `Test@12345` (متصل به یک «شرکت تست عیب‌یابی» که خودش هم در همان seed ساخته می‌شود)

اما چون طبق بند ۱ بالا `prisma db seed` هیچ‌وقت خودکار روی production اجرا نشده، **این دو حساب هیچ‌وقت واقعاً در دیتابیس production ساخته نشده‌اند**. تنها حساب‌هایی که واقعاً روی production کار می‌کنند، آن‌هایی هستند که در ابتدای پروژه (اولین باری که دیتابیس راه‌اندازی شد) به هر شکلی واقعاً seed شدند — یعنی `admin` (ادمین اصلی) و `azma-post` (حساب شرکت نمونه).

**این وضعیت باید یکی از این دو مسیر را طی کند:**
- **گزینه ۱ — حذف:** اگر قرار نیست به‌زودی از Shell سرور برای seed دستی استفاده شود، این دو حساب تست را از `seed.ts` حذف کنید — چون در حالت فعلی صرفاً گمراه‌کننده‌اند (هرکسی این فایل را بخواند فکر می‌کند این حساب‌ها روی production فعال‌اند).
- **گزینه ۲ — ساخت دستی:** اگر واقعاً به این حساب‌های تست نیاز دارید (مثلاً برای عیب‌یابی مشابه آنچه قبلاً برایش ساخته شدند)، باید دقیقاً مثل بند migration دستی بالا، یک اسکریپت Node با `pg` بنویسید که این دو ردیف را مستقیم در جدول‌های `employees` و `companies`/`company_accounts` درج کند (رمز باید با `bcryptjs` هش شود، دقیقاً مثل چیزی که `seed.ts` انجام می‌دهد — نمونه‌ی این الگو در بخش بعد آمده).

**نکته‌ی جانبی مرتبط و مهم:** حتی حساب اصلی `admin` هم اگر یک‌بار رمزش به هر روشی (دستی یا ...) عوض شده باشد، `seed.ts` **هیچ‌وقت آن را بازنویسی نمی‌کند** — چون منطق فایل این‌طور است: *«اگر کاربری با این username از قبل وجود دارد، هیچ کاری نکن»* (`if (!adminExists) { ...create... }`). یعنی اگر با رمز مستند‌شده در این فایل (`badro@admin1404`) نتوانستید وارد شوید، دلیلش لزوماً باگ نیست — ممکن است رمز واقعی دیتابیس با آنچه اینجا نوشته متفاوت باشد.

### ۴. ربات استعلام خودکار (`page_automation`) — موتور واقعی نوشته شد، ولی به هیچ شرکت واقعی وصل نیست

از ۲۰۲۶-۰۷-۳۱، `src/lib/pricing/page-automation-provider.ts` دیگر یک Interface خالی نیست — با **Playwright واقعی** پیاده‌سازی شده: صفحه‌ی شرکت را باز می‌کند، فیلدهای پیکربندی‌شده در `CompanyForm` (پنل ادمین) را پر می‌کند، چک‌باکس‌های لازم را تیک می‌زند، دکمه‌ی محاسبه را کلیک و قیمت را از selector نتیجه استخراج می‌کند. تمام مراحل تایم‌اوت‌دار هستند (ناوبری ۱۵ ثانیه، هر اکشن دیگر ۱۰ ثانیه) و هر خطا (selector پیدا نشد، صفحه لود نشد، قیمت قابل‌استخراج نبود) به‌جای کرش، `available: false` با `reason` مشخص برمی‌گرداند.

**⚠️ محدودیت مهم حین توسعه — چرا این موتور با شرکت واقعی «دکاپست» تست نشد:**

درخواست اولیه این بود که موتور مستقیماً روی `price.dekapost.ir` (شرکت دکاپست) پیاده و تست شود. اما **این sandbox (محیط اجرای Claude Code) دسترسی شبکه به `price.dekapost.ir` را در سطح پروکسی خروجی مسدود کرده** — این یک policy denial سازمانی است، نه محدودیتی از طرف خودِ سایت دکاپست. این مسئله با سه ابزار مختلف تایید شد: `curl` (خطای `403`)، `WebFetch` (خطای `403`)، و حتی یک مرورگر Chromium واقعی از طریق Playwright (خطای `ERR_TUNNEL_CONNECTION_FAILED`). یعنی **هیچ کدام از ابزارهای این نشست نمی‌توانستند صفحه‌ی واقعی را باز کنند** تا selectorهای دقیق ۹ فیلد و ۲ چک‌باکسی که کاربر توصیف کرده بود (شهر مبدا، شهر مقصد، وزن، ارزش کالا، نوع محتوا، طول، عرض، ارتفاع، «قبول از محل مشتری»، «مرسوله نیاز به بسته‌بندی ندارد») استخراج شود.

**به‌جای حدس‌زدن selectorهای غلط (که در production بی‌صدا شکست می‌خورد)**، مسیر زیر طی شد:
1. موتور به‌صورت کاملاً **عمومی و پیکربندی‌محور** طراحی شد (نه هاردکد برای دکاپست) — هر فیلد شناخته‌شده (`origin`, `destination`, `weight`, `declaredValue`, `length`, `width`, `height`, `contentType`) در `AutomationConfig.fieldSelectors` یک CSS selector می‌گیرد؛ `checkboxSelectors` آرایه‌ای از selector چک‌باکس‌هایی است که باید همیشه تیک بخورند؛ `extraStaticFields` برای فیلدهای خاص یک شرکت که در این لیست از پیش پیش‌بینی نشده (مثلاً یک فیلد نهم ناشناخته). این ساختار الان در `CompanyForm.tsx` (پنل ادمین) هم به‌طور کامل قابل ویرایش است — با UI افزودن/حذف ردیف برای چک‌باکس‌ها و فیلدهای اضافه (مثل الگوی از پیش موجود جدول پله‌ای وزن).
2. چون نه «نوع محتوا» و نه دو چک‌باکس در مدل سفارش بادرو معادل مستقیم ندارند (مشتری بادرو این‌ها را انتخاب نمی‌کند)، این‌ها به‌صورت **مقدار ثابتِ تعریف‌شده توسط ادمین** پر می‌شوند (`contentTypeValue`، و چک‌باکس‌ها همیشه تیک می‌خورند). فیلدهای دیگر (وزن، طول، عرض، ارتفاع، ارزش کالا) از داده‌ی واقعی سفارش/درخواست قیمت می‌آیند — که برای این کار، `PriceQuoteInput`/`QuoteRequest` (در `src/lib/pricing/types.ts` و `engine.ts`) با فیلدهای `lengthCm`, `widthCm`, `heightCm`, `declaredValue` تکمیل شد و در همه‌ی نقاط فراخوانی (`results/page.tsx`، `ParcelDetailsStep.tsx`، `actions/orders.ts`) هم به‌درستی پاس داده می‌شود — قبلاً این مقادیر در فرم سفارش جمع‌آوری می‌شدند ولی هیچ‌وقت به موتور قیمت‌گذاری نمی‌رسیدند.
3. برای اطمینان از درستی خودِ منطق (fill کردن select/input، تیک زدن چک‌باکس، کلیک دکمه، استخراج و parse قیمت فارسی، و مسیرهای خطا)، یک صفحه‌ی HTML آزمایشی محلی با ساختار مشابه (۸ فیلد + ۲ چک‌باکس + دکمه + نتیجه) ساخته و همان منطق دقیق کد production رویش اجرا شد — **نه روی دکاپست واقعی**. نتیجه: مسیر موفق قیمت را درست استخراج کرد (`۱۲۵,۰۰۰ تومان` → `125000`)، و هر دو مسیر خطای عمدی (selector نتیجه‌ی اشتباه، آدرس اشتباه) بدون کرش `available:false` با پیام خطای مشخص برگرداندند.
4. شرکت «دکاپست» **در دیتابیس ساخته نشده** (نه در seed، نه دستی) — چون بدون selector واقعی، ساخت یک ردیف `Company`/`PricingRule` با تنظیمات نصفه‌کاره صرفاً گمراه‌کننده بود.

**برای تکمیل واقعی اتصال دکاپست، این مراحل باقی مانده (نیاز به کسی با دسترسی شبکه‌ی واقعی به `price.dekapost.ir` دارد):**
1. صفحه‌ی `price.dekapost.ir` را در مرورگر معمولی باز کنید، با Inspect Element (کلیک راست → بازرسی) روی هر ۹ فیلد و ۲ چک‌باکس، selector دقیق (id یا attribute پایدار) هرکدام را پیدا کنید.
2. از `/admin/companies/new` یک شرکت جدید به نام «دکاپست» بسازید، «نحوه دریافت قیمت» را روی «ربات استعلام خودکار» بگذارید، و selectorهای واقعی را در فرم (که حالا همه‌ی ۹ فیلد + چک‌باکس‌ها + فیلدهای اضافه را پشتیبانی می‌کند) وارد کنید.
3. یک استعلام واقعی از `/results` تست کنید تا مطمئن شوید قیمت واقعی دکاپست برمی‌گردد.

**⚠️ ریسک جدید و مهم برای دیپلوی — باینری Chromium روی Liara از قبل نصب نیست:**

`playwright` به‌عنوان dependency اضافه شد، ولی خودِ باینری مرورگر Chromium (چند صد مگابایت) یک artifact جدا از npm package است و باید جداگانه دانلود/نصب شود (`npx playwright install --with-deps chromium`). این sandbox این باینری را از پیش نصب‌شده دارد (`PLAYWRIGHT_BROWSERS_PATH`)، **ولی محیط production روی Liara چنین چیزی ندارد** — دقیقاً همان کلاس مشکلی که در بخش ⚠️۱ برای `scripts/start.sh` توضیح داده شد: بدون یک Dockerfile اختصاصی یا Release Command که این نصب را قبل از اجرای سرور انجام دهد، `chromium.launch()` روی production با خطای «Executable doesn't exist» شکست می‌خورد و `page_automation` برای همه‌ی شرکت‌ها همیشه `available: false` برمی‌گرداند (بدون کرش کلی سایت، چون خطا مدیریت شده — ولی عملاً غیرفعال). این باید قبل از فعال کردن `page_automation` برای هر شرکت واقعی روی production حل شود. همچنین اجرای یک مرورگر headless داخل Server Action یک هزینه‌ی محسوس CPU/RAM/زمان (چند ثانیه به‌ازای هر استعلام) دارد که برای مقایسه‌ی هم‌زمان قیمت چند شرکت (مسیر `/results`) باید سنجیده شود.

### ۵. حادثه‌ی قطعی سایت (۲۰۲۶-۰۸-۰۱) — علت واقعی، اصلاح و تاریخچه‌ی کامل ماجرا

این بخش برای این نوشته شده که تاریخچه‌ی کامل یک اشتباه تشخیصی ثبت بماند — چون علت اولیه‌ای که فکر می‌کردیم درست بود، غلط از آب درآمد.

**مرحله ۱ — پیاده‌سازی چاپار و اولین گزارش خرابی:** در ۲۰۲۶-۰۸-۰۱ (کامیت‌های `a156c3d`/`1eff4a7`) اتصال واقعی چاپار (قیمت + رهگیری) پیاده‌سازی و push شد. بلافاصله بعد از دیپلوی، کاربر گزارش داد ثبت سفارش (هم پستی هم پیک موتوری) با Internal Server Error از کار افتاده. **حدس اولیه (که در همین سند نوشته شده بود) این بود که علت، تغییرات چاپار است** — طبق دستور فوری کاربر، هر دو commit با `git revert` برگردانده شد (کامیت `c4f0985`)، با تایید این‌که کد بعد از revert بایت‌به‌بایت با `f93044f` (آخرین کامیت قبل از چاپار) یکسان است.

**مرحله ۲ — علت واقعی از لاگ Liara پیدا شد، ربطی به چاپار نداشت:** کاربر از لاگ واقعی سرور Liara این خطا را پیدا کرد:
```
Error: Failed to load external module playwright-...
Cannot find module '/app/node_modules/playwright-core/browsers.json'
```
یعنی علت واقعی، **کامیت `556ea8b` (پیاده‌سازی `page_automation` با Playwright برای دکاپست، از قبل از چاپار)** بود، نه چاپار. `src/lib/pricing/page-automation-provider.ts` در بالای فایل یک `import { chromium } from "playwright";` **ثابت (static)** داشت. چون خودِ پکیج `playwright-core` روی build استاندالون production لیارا کامل کپی نشده (فایل داخلی‌اش `browsers.json` گم بوده — دقیقاً همان کلاس مشکل «باینری Chromium روی Liara نصب نیست» که در همین سند از قبل مستند شده بود، ولی حتی بدتر: مشکل فقط به باینری مرورگر محدود نبود، خودِ import ماژول `playwright` هم شکست می‌خورد)، **این import ثابت در سطح ماژول throw می‌کرد**. چون `src/lib/pricing/engine.ts` این فایل را import می‌کند و بدون قید‌و‌شرط `new PageAutomationProvider()` می‌سازد، و خودِ `engine.ts` را **همه‌ی مسیرهای قیمت‌گذاری** (صفحه نتایج، پیش‌نمایش قیمت تقریبی، ثبت نهایی سفارش — هم پستی هم پیک موتوری) import می‌کنند، این یک خطای سطح ماژول بود که کل زنجیره‌ی import را خراب می‌کرد — **کاملاً مستقل از این‌که شرکت «دکاپست» اصلاً در دیتابیس وجود داشته باشد یا کوئری بشود یا نه**. یعنی سایت از همان لحظه‌ی دیپلوی `556ea8b` (حتی قبل از چاپار) خراب بوده؛ revert کردن چاپار مشکل را حل نکرده بود (چون `page-automation-provider.ts` هیچ‌وقت revert نشد) — به‌احتمال زیاد deploy واقعی روی Liara دیرتر از commit اتفاق افتاده و زمان‌بندی گزارش کاربر باعث اشتباه در تشخیص علت شد.

**مرحله ۳ — اصلاح واقعی (۲۰۲۶-۰۸-۰۱، بعد از این حادثه):**
1. **`page-automation-provider.ts`:** `import { chromium } from "playwright"` ثابت حذف و به `const { chromium } = await import("playwright");` **پویا، داخل try/catch** تبدیل شد — یعنی حتی اگر ماژول playwright روی production باز هم قابل‌بارگذاری نباشد، خطا همان‌جا گرفته می‌شود و به سطح ماژول (و در نتیجه به `engine.ts`) سرایت نمی‌کند.
2. **کلید خاموش/روشن (`PAGE_AUTOMATION_ENABLED = false`)** به بالای همین فایل اضافه شد — `getQuote()` قبل از هر کاری این را چک می‌کند و اگر `false` باشد، بدون لمس Playwright/دیتابیس، فوراً `available:false` برمی‌گرداند. تا وقتی نصب Chromium روی production حل نشود، این `true` نشود.
3. **`engine.ts`:** یک تابع مشترک `safeGetQuote()` اضافه شد که هر فراخوانی `provider.getQuote()` (برای همه‌ی providerها، نه فقط page_automation) را try/catch می‌کند — یعنی از این به بعد، اگر هر provider (به هر دلیلی، در حال یا آینده) throw کند، فقط همان شرکت با `available:false` از نتایج حذف می‌شود، نه این‌که کل درخواست/صفحه کرش کند. این دفاع در عمق است؛ کلید خاموش/روشن بالا دلیل اصلی امن‌بودن فعلی است.
4. **`CompanyForm.tsx`:** گزینه‌ی «ربات استعلام خودکار» از دراپ‌داون «نحوه دریافت قیمت» حذف شد (دیگر برای شرکت جدید/شرکتی که این گزینه را نداشت قابل‌انتخاب نیست) تا این مسیر عملاً به‌طور کامل از UI هم بسته باشد.

**نتیجه (در آن لحظه):** فایل‌های چاپار که در مرحله ۱ revert شدند، عمداً دوباره اضافه نشدند تا وقتی علت اصلی (page_automation) درست تشخیص و رفع شود. `page_automation` (دکاپست) کاملاً بی‌خطر شد — هم کلید خاموش/روشن، هم import پویا، هم UI مسدود — ولی هنوز از کد حذف نشده، فقط غیرفعال شده، تا وقتی مشکل نصب Chromium روی Liara به‌طور رسمی حل شود. (چاپار بعداً، در بخش ⚠️۶، با احتیاط بیشتر دوباره پیاده‌سازی شد.)

### ۶. اتصال چاپار — بازپیاده‌سازی دوم، این‌بار با تست مقاومت در برابر خطای شبکه قبل از push

بعد از رفع علت واقعی حادثه‌ی بخش ⚠️۵ (که مربوط به `page_automation` بود، نه چاپار)، اتصال چاپار دوباره پیاده‌سازی شد — این‌بار با چند لایه‌ی ایمنی اضافه که در تلاش اول نبود:

1. **بدون هیچ `import` ثابت وابسته به بسته‌ی خارجی سنگین:** برخلاف `page_automation` (که با Playwright این مشکل را ایجاد کرد)، چاپار فقط از `fetch` استفاده می‌کند — یک global استاندارد Node/Next، بدون هیچ ریسک شکست بارگذاری ماژول در سطح build استاندالون.
2. **دو لایه‌ی try/catch به‌جای یک لایه:** هم `ExternalApiProvider.getQuote()` و بلوک چاپار در `trackOrderAction()` کل بدنه‌شان را try/catch می‌کنند، هم `engine.ts` با `safeGetQuote()` (که از حادثه‌ی قبلی مانده) دوباره همه‌چیز را می‌پوشاند. یعنی حتی اگر یک لایه فراموش شود، لایه‌ی دیگر همچنان از crash جلوگیری می‌کند.
3. **تست محلی مقاومت در برابر خطای شبکه (قبل از push):** با mock کردن `global.fetch` برای throw کردن خطاهای واقعی (`ECONNREFUSED`، `AbortError`/تایم‌اوت)، تایید شد که:
   - `getChaparQuote`/`getChaparTracking` (در `client.ts`) این خطاها را به‌صورت یک `Error` عادی و قابل‌catch throw می‌کنند (نه چیزی که process را crash کند).
   - در یک batch شبیه‌سازی‌شده با سه «شرکت» (یکی با خطای شبکه‌ی چاپار، یکی فرمول داخلی سالم، یکی page_automation غیرفعال) از طریق `Promise.all` مثل `engine.ts`، نتیجه دقیقاً همان چیزی بود که باید باشد: شرکت چاپار با `available:false` حذف شد، شرکت فرمول داخلی قیمتش را برگرداند، **و `Promise.all` اصلاً reject نشد.**

**⚠️ محدودیت‌های همچنان پابرجا (از تلاش اول):** این تست‌ها منطق مقاومت در برابر خطا را تایید می‌کنند، نه صحت داده‌ی واقعی چاپار — چون این sandbox هنوز به `app.krch.ir` دسترسی شبکه ندارد (مسدود توسط پروکسی خروجی، مثل قبل)، **هیچ تست واقعی با API زنده‌ی چاپار انجام نشد**. شکل دقیق پاسخ `get_quote`/`tracking` و کد `method` سرویس هنوز تایید نشده — همان جزئیات و مراحل باقی‌مانده‌ای که قبلاً مستند شده بود (تست واقعی از یک محیط با دسترسی شبکه‌ی واقعی، بعد از ساخت شرکت «چاپار» در پنل ادمین) هنوز صادق است.

بدون migration — دقیقاً همان ساختار قبلی (credentials در `Company.apiKey` با فرمت `username:password`، نگاشت شهر با کش درون‌حافظه‌ای).

---

## ۱. توضیح کامل پروژه

بادرو پلتفرم مقایسه قیمت و ثبت سفارش ارسال مرسوله در ایران است — هم پستی بین‌شهری، هم پیک درون‌شهری. مشتری مبدا/مقصد و مشخصات مرسوله را وارد می‌کند، قیمت چند شرکت طرف‌قرارداد را همزمان می‌بیند، و سفارش را بدون پرداخت آنلاین ثبت می‌کند (تسویه‌حساب مستقیم با شرکت در مبدا یا مقصد انجام می‌شود).

### استک فنی

| لایه | تکنولوژی |
|---|---|
| فرانت‌اند + بک‌اند | Next.js 16.2.10 (App Router + Turbopack)، TypeScript، React 19.2.4 — تک‌کدبیس فول‌استک |
| استایل | Tailwind CSS v4 |
| دیتابیس | PostgreSQL |
| ORM | Prisma 7.8 با `@prisma/adapter-pg` (driver adapter؛ `datasource.url` از `prisma.config.ts`/env خوانده می‌شود، نه از `schema.prisma`) |
| احراز هویت | JWT (`jose`) — سه کوکی جدا برای مشتری/کارمند/شرکت: `badro_customer_session`, `badro_staff_session`, `badro_company_session`. رمزهای عبور با `bcryptjs` هش می‌شوند (`src/lib/auth/password.ts`) |
| نقشه | Leaflet / OpenStreetMap، به‌همراه دکمه‌ی Geolocation مرورگر («موقعیت من») |
| نمودار | Recharts |
| آیکون | lucide-react |
| PWA | `manifest.ts` + service worker دستی (`public/sw.js`) — بدون رفتار مسیر‌محور خاص |
| SMS | لایه انتزاعی `SmsProvider` (`src/lib/sms/`) — پیاده‌سازی‌های موجود: `mock` و `kavenegar` (واقعی، جزئیات در بخش ۲) |
| میزبانی | **Liara**، بدون Dockerfile (کاملاً auto-detect-محور). محدودیت‌های مهمش در بخش ⚠️ بالا توضیح داده شده |

نکته: در Next.js 16 نام `middleware.ts` به `proxy.ts` تغییر کرده — فایل معادل پروژه: `src/proxy.ts`.

### ساختار کلی

```
src/
├── app/                       # صفحات (App Router)
│   ├── (public)/               # صفحه اصلی، نتایج، سفارش، رهگیری، درباره/تماس/قوانین
│   ├── admin/                   # پنل ادمین/کارمندان (route group (dashboard) + login)
│   ├── company/                  # پنل شرکت‌ها (route group (dashboard) + login + test)
│   └── panel/                     # پنل مشتری
├── actions/                    # Server Actions — تنها نقطه‌ی ورود عملیات نوشتنی (معادل لایه API)
│   ├── admin/                   # companies.ts، employees.ts، orders.ts، content.ts
│   ├── auth.ts                   # لاگین/خروج هر سه نقش
│   ├── company-orders.ts          # تغییر وضعیت سفارش از پنل شرکت
│   ├── company-profile.ts          # تغییر رمز عبور خودِ شرکت
│   ├── orders.ts, quote.ts, profile.ts, contact.ts, tracking.ts
├── lib/
│   ├── pricing/                  # موتور قیمت‌گذاری — جزئیات کامل در بخش ۲
│   ├── auth/                      # session (JWT هر نقش)، OTP، password (bcryptjs)، RBAC کارمندان
│   ├── sms/                        # لایه انتزاعی ارسال پیامک: provider.ts + kavenegar-provider.ts
│   └── prisma.ts                    # singleton کلاینت Prisma (با adapter-pg)
├── components/
│   ├── admin/                    # CompanyForm، EmployeeForm و فرم‌های ادمین
│   ├── company/                   # ChangePasswordForm پنل شرکت
│   ├── order/                      # AddressFields، MapPicker، OrderForm، ParcelRow
│   ├── home/                        # HomeServiceFlow (ویزارد صفحه اصلی)، ParcelDetailsStep
│   └── ...
└── proxy.ts                        # میان‌افزار احراز هویت مسیرها (جایگزین middleware.ts قدیمی)

prisma/
├── schema.prisma      # ~۱۵ مدل کامل (User, Employee, Company, CompanyAccount, Order, PricingRule, ...)
├── seed.ts             # داده اولیه idempotent — طبق بخش ⚠️۱، خودکار روی production اجرا نمی‌شود
└── migrations/         # طبق بخش ⚠️۱، خودکار روی production اعمال نمی‌شوند؛ نیاز به اجرای دستی دارند
```

معماری نوشتن داده فقط از طریق Server Actions است — هیچ Route Handler واقعی برای نوشتن وجود ندارد.

---

## ۲. وضعیت کامل فیچرها

### ✅ کاملاً تکمیل و فعال

| فیچر | جزئیات |
|---|---|
| سه پنل | مشتری (`/panel`، OTP)، ادمین/کارمندان (`/admin`، یوزرنیم/پسورد + RBAC جدول‌محور)، شرکت‌ها (`/company`، یوزرنیم/پسورد) |
| موتور قیمت‌گذاری `internal_formula` | فرمول ریاضی ساده + جدول پله‌ای؛ ورودی وزن بر حسب **گرم**، تعرفه‌های ادمین (`pricePerKg`, `weightTiers`) همچنان بر مبنای کیلوگرم — تبدیل داخل `internal-formula-provider.ts` انجام می‌شود |
| چرخه سفارش | ثبت → تغییر وضعیت (توسط شرکت/ادمین) → پیگیری با کد رهگیری → تاریخچه کامل وضعیت |
| مدیریت حساب کاربری شرکت‌ها | ادمین از `CompanyForm` می‌تواند یوزرنیم/رمز `CompanyAccount` هر شرکت را بسازد/ویرایش کند؛ خودِ شرکت هم از `/company/settings` می‌تواند رمزش را تغییر دهد |
| گزارش‌ها | گزارش کمیسیون ادمین (نمودار + CSV) + گزارش مالی هر شرکت |
| مدیریت محتوا | اسلایدر صفحه اصلی از `/admin/content` |
| فرم ثبت سفارش | آدرس فقط با هشدار لحظه‌ای برای حروف غیرفارسی (نه فیلتر بی‌صدا)، دکمه‌ی Geolocation «موقعیت من»، الزام واقعی انتخاب موقعیت روی نقشه (پیک: مبدا+مقصد اجباری، پستی: فقط مبدا اجباری)، اسکرول خودکار به بالا بین مراحل ویزارد |
| SMS واقعی (کاوه‌نگار) | `KavenegarSmsProvider` به REST API واقعی وصل می‌شود — ولی فقط با **خط رایگان/تست** امتحان شده؛ ارسال به شماره‌های دیگر با خطای ۵۰۱ مواجه می‌شود چون نیاز به خرید خط اختصاصی دارد |
| `PricingSourceType.external_api` — اتصال چاپار (بازپیاده‌سازی دوم) | `external-api-provider.ts` واقعاً به `app.krch.ir` وصل می‌شود (قیمت + رهگیری در `/tracking`)؛ فقط با `fetch` (بدون وابستگی خارجی سنگین)؛ دو لایه try/catch؛ با تست شبیه‌سازی‌شده‌ی خطای شبکه (ECONNREFUSED/timeout) تایید شد که کل سایت را نمی‌ترکاند (بخش ⚠️۶) | **هنوز با API واقعی چاپار تست نشده** (دسترسی شبکه‌ی sandbox مسدود است). کد `method` سرویس و شکل دقیق پاسخ تایید نشده — جزئیات در بخش ⚠️۶ |
| `PricingSourceType.page_automation` | موتور واقعی با Playwright نوشته شد؛ بعد از یک حادثه‌ی قطعی سایت (بخش ⚠️۵) عمداً **غیرفعال شد**: کلید `PAGE_AUTOMATION_ENABLED=false` در `page-automation-provider.ts`، import پویا (نه ثابت) برای جلوگیری از کرش کل ماژول قیمت‌گذاری، و گزینه‌اش از دراپ‌داون `CompanyForm` حذف شده | **باید فعال نماند** تا نصب Chromium روی production لیارا رسماً حل شود (بخش ⚠️۴/⚠️۵). بعد از آن: `PAGE_AUTOMATION_ENABLED=true`، گزینه به دراپ‌داون برگردد، شرکت «دکاپست» با selector واقعی ساخته و تست شود |

### ❌ هنوز شروع نشده

| فیچر | توضیح |
|---|---|
| زون‌بندی قیمت (Zone-based pricing) | مدل داده فعلی بر پایه‌ی «فاصله»ی محاسبه‌شده از `CityDistanceIndex` است، نه «زون». برای پیاده‌سازی نیاز به مدل‌های جدید (`Zone`, نگاشت شهر→زون, جدول قیمت زون×زون) و migration واقعی دارد — جزئیات کامل طراحی در تاریخچه‌ی مکالمات قبلی بحث شده اما هیچ کدی نوشته نشده |
| اتصال واقعی به شرکت‌های حمل‌ونقل واقعی (مثل تیپاکس) | تمام شرکت‌های فعلی در `seed.ts` نمونه/ساختگی‌اند (آزما پست، پیک تیز پرواز، …) — نه نام واقعی شرکت‌ها ثبت شده، نه فرمول قیمت واقعی‌شان، نه API/ربات واقعی‌شان وصل شده |
| درگاه پرداخت آنلاین | عمداً خارج از محدوده — تسویه‌حساب مستقیم/حضوری با شرکت |
| SMS provider دوم (ملی‌پیامک) | فقط کامنت placeholder در `src/lib/sms/provider.ts`، هیچ کلاسی نوشته نشده |
| اپلیکیشن موبایل نیتیو | فقط PWA وجود دارد |

---

## ۳. راهنمای اجرای migration دستی

این راهنما دقیقاً همان روشی است که برای حادثه‌ی وزن (بخش ⚠️۲) استفاده شد. تا وقتی محدودیت `scripts/start.sh` (بخش ⚠️۱) رفع نشده، هر تغییر ساختاری روی دیتابیس production باید از همین مسیر انجام شود.

### مرحله ۱ — وصل شدن به Shell سرور از پنل Liara

1. وارد پنل Liara شوید و اپلیکیشن مربوط به بادرو را باز کنید.
2. به تب **«خط فرمان»** (Shell/Console) بروید — این یک ترمینال زنده داخل همان کانتینر در حال اجرا است.
3. مطمئن شوید در working directory درستی هستید (معمولاً همان جایی که `node_modules` و فایل‌های standalone قرار دارند — دستور `ls` بزنید تا مطمئن شوید `node_modules/pg` وجود دارد).

### مرحله ۲ — نوشتن اسکریپت اصلاحی با heredoc

از الگوی دقیق زیر استفاده کنید (`fix.js` فقط یک اسم نمونه است، هر اسمی می‌تواند باشد):

```sh
cat > fix.js << 'EOF'
const { Client } = require('pg');

async function main() {
  const client = new Client({ connectionString: process.env.DATABASE_URL });
  await client.connect();
  try {
    // SQL مورد نظر را اینجا بنویسید — مثال:
    await client.query('ALTER TABLE orders ALTER COLUMN "weightGrams" TYPE INTEGER USING ROUND("weightGrams")::INTEGER;');
    console.log('OK');
  } finally {
    await client.end();
  }
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
EOF
```

نکات مهم این مرحله:
- استفاده از `'EOF'` (با کوتیشن) نه `EOF` ساده، تا شل هیچ متغیری داخل متن اسکریپت را expand نکند.
- `process.env.DATABASE_URL` همان env var ای است که خودِ اپ هم برای اتصال به دیتابیس استفاده می‌کند — نیازی به وارد کردن دستی connection string (و لو رفتنش در تاریخچه‌ی ترمینال) نیست.
- پکیج `pg` نیازی به نصب جداگانه ندارد — چون `pg` از قبل یکی از dependency های پروژه است (برای `@prisma/adapter-pg`) و در build/standalone موجود است.

### مرحله ۳ — اجرا و بررسی نتیجه

```sh
node fix.js
```

اگر پیام خطا دید، **قبل از تلاش دوباره** دقیقاً بفهمید SQL چرا خطا داد (مثلاً سرریز عددی، ستون وجود ندارد، قفل روی جدول). هیچ‌وقت SQL تغییردهنده را کورکورانه دوباره اجرا نکنید.

برای تایید نتیجه، یک اسکریپت کوچک دیگر (یا همان الگو با یک `SELECT`) بنویسید و مقادیر واقعی را چاپ کنید — هیچ ابزار مانیتورینگ گرافیکی روی این محیط در دسترس نیست، پس تایید باید از طریق خروجی همین اسکریپت‌ها باشد.

### مرحله ۴ — پاک‌سازی

```sh
rm fix.js
```

فایل موقت را پاک کنید تا روی فایل‌سیستم کانتینر باقی نماند (هرچند با هر دیپلوی جدید فایل‌سیستم از نو ساخته می‌شود، پاک‌سازی دستی هم عادت خوبی است — خصوصاً وقتی اسکریپت شامل SQL حاوی جزئیات ساختار داخلی دیتابیس است).

### هشدارهای کلی

- **هیچ rollback خودکاری وجود ندارد.** قبل از هر `ALTER`/`UPDATE`، دقیقاً مطمئن شوید SQL درست است — ترجیحاً اول با یک `SELECT` مشابه، دامنه‌ی تأثیر را روی چند ردیف تست کنید.
- **این مسیر برای `prisma migrate deploy`/`prisma db seed` هم قابل استفاده نیست** — چون Prisma CLI روی این محیط نصب نیست. برای این دو دستور خاص، تنها راه واقعی، اجرا از یک محیط دیگر (لپ‌تاپ توسعه‌دهنده یا CI) با `DATABASE_URL` موقتاً روی مقدار production است، نه از همین Shell.
- بعد از هر migration دستی، **حتماً** یک migration رسمی متناظر در `prisma/migrations/` بسازید (اگر از قبل نساخته‌اید) تا `schema.prisma` و تاریخچه‌ی migration ها با واقعیتِ دیتابیس هماهنگ بمانند — و طبق بخش ⚠️۲(ج)، وقتی به محیطی با Prisma CLI دسترسی داشتید، `prisma migrate resolve --applied <نام migration>` را هم اجرا کنید تا بوکیپینگ Prisma هم هماهنگ شود.

---

## ۴. تاریخچه‌ی تغییرات

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — حذف مسیر تشخیصی `/api/debug`
مسیر `src/app/api/debug/route.ts` حذف شد. این یک روت تشخیصی موقت بود که بدون هیچ احراز هویتی شکل مقدار `DATABASE_URL` و نتیجه‌ی یک تلاش اتصال به دیتابیس را برمی‌گرداند — نشتی اطلاعاتی داخلی روی یک اندپوینت عمومی. ارجاعات این مسیر در `README.md` و `scripts/start.sh` هم پاک‌سازی شد.
**کامیت:** `f17f1d8`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — اتصال واقعی کاوه‌نگار برای ارسال OTP
کلاس واقعی `KavenegarSmsProvider` در `src/lib/sms/kavenegar-provider.ts` اضافه شد که به REST API کاوه‌نگار وصل می‌شود (`POST` به `sms/send.json`). در `src/lib/sms/provider.ts`، وقتی متغیر محیطی `SMS_PROVIDER=kavenegar` باشد، این کلاس به‌جای `MockSmsProvider` استفاده می‌شود. دو متغیر محیطی جدید لازم دارد: `KAVENEGAR_API_KEY` و `KAVENEGAR_SENDER` (به `.env.example` هم اضافه شدند، بدون مقدار واقعی).

**نکته مهم/محدودیت فعلی:** این اتصال فقط با **خط رایگان (تست) کاوه‌نگار** امتحان شده. ارسال به شماره‌های دیگر (غیر از شماره‌ی مالک اکانت/خط تست) با **خطای ۵۰۱** از سمت کاوه‌نگار مواجه می‌شود، چون خط رایگان محدودیت دارد و برای ارسال واقعی به مشتریان باید **خط اختصاصی خریداری شود**.

**کامیت:** `ae8a397`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — پنج بهبود در فرم ثبت سفارش

1. **وزن مرسوله از کیلوگرم به گرم:** فیلد ورودی وزن در ارسال پستی حالا گرم می‌گیرد. ستون `Order.weightKg` (Decimal) به `Order.weightGrams` (Int) تغییر نام و نوع داد — با migration که مقادیر سفارش‌های قبلی را با ضرب در ۱۰۰۰ به گرم تبدیل می‌کند تا داده‌ای از دست نرود. تعرفه‌های ادمین (`pricePerKg` در `PricingRule.formulaParams`، و `weightTiers`) همچنان بر مبنای کیلوگرم تعریف می‌شوند و دست‌نخورده ماندند؛ تبدیل گرم→کیلوگرم فقط داخل `internal-formula-provider.ts` و درست لحظه‌ی محاسبه قیمت انجام می‌شود. **⚠️ این migration هیچ‌وقت خودکار روی production اجرا نشد — جزئیات کامل حادثه و اقدام لازم در بخش ⚠️۲ همین سند.**
2. **محدودیت تایپ فارسی در آدرس:** در فیلدهای خیابان/کوچه/پلاک/طبقه/توضیحات، حروف انگلیسی حین تایپ به‌صورت خودکار حذف می‌شوند. علاوه‌براین، قبل از رفتن به مرحله بعد (یا ثبت نهایی سفارش)، اگر باز هم متن انگلیسی در آدرس تشخیص داده شود (مثلاً از طریق paste)، پیام «لطفا فارسی تایپ کنید» نمایش داده و ادامه مسدود می‌شود. (**این رفتار بعداً در تغییر بعدی اصلاح شد — به مورد بعدی نگاه کنید.**)
3. **دکمه «موقعیت من»:** زیر نقشه انتخاب آدرس (`MapPicker`) اضافه شد؛ با Geolocation مرورگر موقعیت فعلی کاربر را می‌گیرد، مارکر را روی همان نقطه می‌گذارد و نقشه را به آن نقطه پن می‌کند.
4. **رفع مشکل اسکرول بین مراحل:** ویزارد صفحه اصلی (`HomeServiceFlow`) هنگام رفتن به هر مرحله جدید (چه «بعدی» چه «بازگشت») صفحه را خودکار به بالا اسکرول می‌کند.
5. **الزام واقعی انتخاب موقعیت روی نقشه:** قبلاً `mapRequired` فقط یک هشدار بصری بود و مانع رفتن به مرحله بعد/ثبت سفارش نمی‌شد. حالا واقعاً اعمال می‌شود: در حالت **پیک موتوری**، انتخاب موقعیت روی نقشه برای هم مبدا هم مقصد اجباری است؛ در حالت **ارسال پستی**، فقط برای مبدا اجباری است و برای مقصد اختیاری می‌ماند (که پیش‌تر اصلاً نقشه‌ای برای مقصد نمایش داده نمی‌شد — حالا نمایش داده می‌شود، فقط اجباری نیست).

**کامیت:** `6a34d18`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — اصلاح رفتار محدودیت فارسی در فیلدهای آدرس

رفتار قبلی (حذف بی‌صدای حروف انگلیسی حین تایپ) گیج‌کننده بود — چون هیچ چیزی روی صفحه ظاهر نمی‌شد، کاربر فکر می‌کرد سایت مشکل دارد. جایگزین شد با:
- تایپ دیگر محدود نمی‌شود؛ کاربر می‌تواند هر متنی (فارسی یا انگلیسی) در فیلدهای خیابان/کوچه/پلاک/طبقه/توضیحات بنویسد.
- اگر متنِ داخل یکی از این فیلدها حرف انگلیسی داشته باشد، بلافاصله (همان لحظه تایپ) پیام خطای «لطفا فارسی تایپ کنید» درست چسبیده به لبه‌ی پایین همان فیلد نمایش داده می‌شود (با prop موجود `error` در کامپوننت `Input`، و به‌صورت دستی برای `textarea` توضیحات).
- تا وقتی حرف انگلیسی در آدرس باقی مانده، دکمه «بعدی» در ویزارد صفحه اصلی غیرفعال می‌ماند (مثل بقیه اعتبارسنجی‌های آن ویزارد)؛ در فرم نهایی ثبت سفارش (`OrderForm`) هم همچنان قبل از ارسال چک و مسدود می‌شود.

**کامیت:** `037eafb`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — حساب‌های تست تشخیصی + صفحه سلامت برای عیب‌یابی لاگین

دو مشکل گزارش شد: (۱) `/admin/login` باز می‌شود ولی با رمز درست هم «رمز اشتباه است» می‌دهد، (۲) `/company/login` روی چند دستگاه/مرورگر مختلف اصلاً باز نمی‌شود (نه یک مشکل کش محلی). بررسی کد نشون داد هیچ تفاوت ساختاری بین مسیر `admin/login` (که کار می‌کنه) و `company/login` (که کار نمی‌کنه) نیست — زنجیره‌ی import، `proxy.ts`، `session.ts`، کامپوننت‌های فرم، همه هم‌شکل و بدون فراخوانی دیتابیسی روی خود صفحات لاگین‌اند؛ چک کاراکترهای نامرئی یونیکد و تطابق حروف بزرگ/کوچک نام فایل‌ها هم چیزی پیدا نکرد. برای عیب‌یابی بیشتر (که نیاز به تست روی دیتابیس/سرور واقعی داشت):

- دو حساب تست تشخیصی به `prisma/seed.ts` اضافه شد (ایده‌مپوتنت، جدا از حساب‌های اصلی `admin`/`azma-post`): `testadmin`/`Test@12345` و `testcompany`/`Test@12345`.
- صفحه‌ی سلامت `GET /company/test` (`src/app/company/test/page.tsx`) اضافه شد — بدون هیچ import اضافه یا فراخوانی دیتابیس، فقط یک متن ساده. در `src/proxy.ts` هم از احراز هویت معاف شد.

**نتیجه‌ی تست‌ها (پیگیری بعدی):** `/company/test` باز شد ✅ (یعنی کل route group `/company` سالم است)، ولی `/company/login` همچنان کاملاً لود نمی‌شود ❌ — و حتی با `testadmin` تازه‌ساز هم `/admin/login` پیام «رمز اشتباه است» می‌دهد. **⚠️ نکته‌ی مهم: حساب‌های تست هیچ‌وقت واقعاً روی production ساخته نشدند چون seed خودکار اجرا نمی‌شود — جزئیات کامل در بخش ⚠️۳ همین سند.** علت اصلی مشکل `/company/login` هنوز به‌طور قطعی مشخص نشده؛ سرنخ‌های بعدی («multirun: one or more of the provided commands ended abnormally» در لاگ Liara) نشان می‌دهد این مشکل به لایه‌ی زیرساخت/پلتفرم Liara مربوط است، نه کد این ریپو (`multirun` هیچ‌جای کد پروژه استفاده نشده — باید بخشی از buildpack خودِ Liara باشد).

هر دو مورد (حساب‌های تست و صفحه‌ی `/company/test`) **تشخیصی‌اند** — بعد از رفع قطعی مشکل باید حذف بشن (دقیقاً مثل الگویی که قبلاً برای `src/app/api/debug` رعایت شد).

**کامیت:** `2ea6fe4`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — تکمیل مدیریت حساب کاربری شرکت‌ها (CompanyAccount)

قبل از این تغییر، مدل `CompanyAccount` در دیتابیس بود ولی **هیچ رابط کاربری‌ای برای ساخت/ویرایش آن وجود نداشت** — تنها راه ساخت یک حساب ورود پنل شرکت، دستکاری مستقیم دیتابیس (مثل `prisma/seed.ts`) بود؛ یعنی اگر ادمین از `/admin/companies/new` یک شرکت واقعی جدید می‌ساخت، آن شرکت اصلاً نمی‌توانست وارد پنل خودش شود. با همان الگوی از قبل پیاده‌شده برای `Employee` (فرم + اکشن با `hashPassword`)، این قابلیت کامل شد:

1. **`CompanyForm.tsx` / `saveCompanyAction`:** دو فیلد «نام کاربری» و «رمز عبور» به فرم ثبت/ویرایش شرکت اضافه شد.
   - ساخت شرکت جدید، یا ویرایش شرکتی که هنوز `CompanyAccount` ندارد (مثلاً شرکت‌های قدیمی‌تر از این تغییر) → رمز عبور اجباری (حداقل ۴ کاراکتر) و یک `CompanyAccount` جدید ساخته می‌شود.
   - ویرایش شرکتی که از قبل حساب دارد → رمز عبور اختیاری؛ اگر خالی بماند، رمز قبلی دست‌نخورده می‌ماند (دقیقاً مثل رفتار فرم کارمند).
   - یکتا بودن نام کاربری هم چک می‌شود (پیام خطای مشخص اگر تکراری باشد).
2. **صفحه‌ی جدید «تنظیمات حساب» در پنل شرکت** (`/company/settings`، لینک جدید در نویگیشن پنل شرکت): فرم استاندارد تغییر رمز عبور (رمز فعلی برای تایید + رمز جدید + تکرار رمز جدید) از طریق اکشن جدید `changeCompanyPasswordAction` (`src/actions/company-profile.ts`).

هیچ migration ای لازم نبود — مدل `CompanyAccount` از قبل در schema وجود داشت، فقط رابط کاربری/منطق نوشتن کم بود.

**کامیت:** `b1fbbc2`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — فعال‌سازی گزینه‌های API و ربات استعلام در فرم شرکت

دراپ‌داون «نحوه دریافت قیمت» در `CompanyForm.tsx` از قبل مقادیر `external_api` و `page_automation` را در enum داشت ولی برچسبشان «(به‌زودی)» بود و انتخابشان هیچ فیلد اضافه‌ای نشان نمی‌داد. حالا:

- **گزینه‌ی «API شرکت»:** با انتخابش، دو فیلد شرطی ظاهر می‌شود — «آدرس API» و «کلید API» (`type="password"` چون حساس است) — که مستقیماً در ستون‌های موجود `Company.apiBaseUrl` و `Company.apiKey` ذخیره می‌شوند.
- **گزینه‌ی «ربات استعلام خودکار» (`page_automation`):** با انتخابش، فیلدهای شرطی زیر ظاهر می‌شوند: آدرس صفحه‌ی استعلام قیمت شرکت، و Selector فرم آن صفحه (فیلد مبدا/مقصد/وزن، دکمه ثبت/محاسبه، محل نمایش نتیجه). این‌ها به‌صورت JSON با ساختار `{ url, fieldSelectors: {origin, destination, weight}, submitSelector, resultSelector }` در ستون موجود `PricingRule.formulaParams` ذخیره می‌شوند (یک ردیف `PricingRule` جدید با `sourceType: "page_automation"`، جدا از ردیف `internal_formula`).

**نکته‌ی مهم:** این تغییر فقط UI و ذخیره‌سازی تنظیمات است. **اجرای واقعی ربات (باز کردن صفحه‌ی شرکت با Playwright/Puppeteer، پرکردن فرم طبق این Selector ها، و خواندن قیمت خروجی) هنوز پیاده‌سازی نشده** — `src/lib/pricing/page-automation-provider.ts` هنوز فقط یک Interface خالی است که همیشه «در دسترس نیست» برمی‌گرداند. همین‌طور `src/lib/pricing/external-api-provider.ts` برای گزینه‌ی API هنوز فراخوانی HTTP واقعی ندارد. این‌ها فاز بعدی هستند؛ همین الان فقط تنظیمات ذخیره می‌شوند تا وقتی آن فاز پیاده شد، داده‌اش از قبل آماده باشد.

بدون migration — همه‌ی ستون‌های لازم (`Company.apiBaseUrl`, `Company.apiKey`, `PricingRule.formulaParams`) از قبل در schema بودند.

**کامیت:** `b5244f7`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — بازنویسی کامل این سند

مستندات به‌طور کامل بازبینی و بازسازماندهی شد تا هر مهندس یا نشست جدید Claude Code بدون خواندن کل تاریخچه‌ی مکالمات، سریع context بگیرد. اضافه شد:
- بخش ⚠️۲: تحلیل کامل حادثه‌ی migration دستی وزن — شامل کشف این‌که SQL دستی اجراشده با migration رسمی یکی نیست (بدون تغییر نوع ستون)، ریسک واقعی سرریز عددی حین `UPDATE` برای سفارش‌های با وزن ≥۱۰ کیلوگرم، و مراحل رفع (تایید نوع ستون، تایید صحت داده، `prisma migrate resolve`).
- بخش ⚠️۳: روشن‌سازی این‌که حساب‌های تست (`testadmin`/`testcompany`) هیچ‌وقت واقعاً روی production ساخته نشدند.
- بخش ۳: راهنمای گام‌به‌گام قابل‌استفاده‌ی مجدد برای اجرای migration دستی از طریق Shell پنل Liara.
- بخش ۲ به‌طور کامل بازسازی شد تا وضعیت هر فیچر (تکمیل/نیمه‌کاره/شروع‌نشده) با جزئیات دقیق مشخص باشد.

### ۱۴۰۵/۰۵/۰۹ (۲۰۲۶-۰۷-۳۱) — پیاده‌سازی واقعی موتور ربات استعلام خودکار (`page_automation`) با Playwright

`playwright` به‌عنوان dependency اضافه شد و `src/lib/pricing/page-automation-provider.ts` از یک Interface خالی به یک موتور واقعی و عمومی تبدیل شد: صفحه‌ی شرکت را باز می‌کند، فیلدها/چک‌باکس‌ها را طبق تنظیمات ذخیره‌شده در `PricingRule.formulaParams` پر می‌کند، دکمه‌ی محاسبه را کلیک و قیمت را استخراج می‌کند — با مدیریت کامل خطا (تایم‌اوت ۱۰-۱۵ ثانیه، هر شکستی `available:false` برمی‌گرداند، نه کرش).

`AutomationConfig` (در `company-form-types.ts`) و UI مربوطه در `CompanyForm.tsx` بازطراحی شد تا عمومی باشد: ۸ فیلد شناخته‌شده (مبدا، مقصد، وزن، ارزش کالا، طول، عرض، ارتفاع، نوع محتوا)، لیست نامحدود چک‌باکس (برای «قبول از محل مشتری»/«نیاز به بسته‌بندی ندارد» و مشابه)، و لیست نامحدود «فیلد اضافه با مقدار ثابت» برای فیلدهای غیرمنتظره‌ی هر شرکت. `PriceQuoteInput`/`QuoteRequest` هم با `lengthCm`, `widthCm`, `heightCm`, `declaredValue` تکمیل شد تا این داده‌ها (که در فرم سفارش از قبل جمع‌آوری می‌شدند ولی به موتور قیمت‌گذاری نمی‌رسیدند) در `results/page.tsx`، `ParcelDetailsStep.tsx` و `actions/orders.ts` هم واقعاً پاس داده شوند.

**⚠️ کار روی شرکت «دکاپست» (`price.dekapost.ir`) که هدف اصلی این درخواست بود، ناتمام ماند** — چون sandbox این نشست دسترسی شبکه به آن دامنه را در سطح پروکسی خروجی مسدود می‌کند (برای هر ابزاری، حتی یک مرورگر Playwright واقعی)، امکان باز کردن صفحه، استخراج selectorهای واقعی، یا اجرای یک تست واقعی وجود نداشت. به‌جای این، منطق موتور روی یک صفحه‌ی HTML آزمایشی محلی هم‌ساختار تست و تایید شد (مسیر موفق + دو مسیر خطا). جزئیات کامل محدودیت، دلایل طراحی، و مراحل باقی‌مانده برای تکمیل واقعی دکاپست (شامل ریسک جدید نصب‌نبودن Chromium روی production) در بخش ⚠️۴ همین سند آمده.

بدون migration — `PricingRule.formulaParams` (`Json?`) از قبل در schema بود، فقط شکل داده‌ی داخلش تغییر کرد.

**کامیت:** `556ea8b`

### ۱۴۰۵/۰۵/۱۱ (۲۰۲۶-۰۸-۰۱) — اتصال چاپار پیاده شد، سایت زنده را خراب کرد، Revert شد

اتصال واقعی چاپار (Chaparnet) برای قیمت (`external-api-provider.ts`) و رهگیری (`tracking.ts` + بازطراحی `/tracking`) پیاده‌سازی و در کامیت‌های `a156c3d`/`1eff4a7` push شد. **بلافاصله بعد از دیپلوی، ثبت سفارش (هم پستی هم پیک موتوری) روی سایت زنده با Internal Server Error از کار افتاد.** کاربر فوراً درخواست revert داد؛ هر دو کامیت با `git revert` برگردانده شد (کامیت `c4f0985`) و تایید شد که کد بعد از revert دقیقاً بایت‌به‌بایت با آخرین حالت سالم (`f93044f`) یکسان است.

**⚠️ اصلاحیه (همان روز):** این تشخیص علت **غلط بود**. علت واقعی چاپار نبود — جزئیات کامل و اصلاح در ردیف بعدی همین تاریخچه و بخش ⚠️۵.

بدون migration — نه پیاده‌سازی اولیه نه revert، هیچ‌کدام تغییر دیتابیسی داشتند.

**کامیت:** `c4f0985`

### ۱۴۰۵/۰۵/۱۱ (۲۰۲۶-۰۸-۰۱) — علت واقعی قطعی سایت پیدا شد: page_automation/Playwright، نه چاپار — اصلاح و غیرفعال‌سازی موقت

از لاگ واقعی Liara علت واقعی پیدا شد: `Cannot find module '/app/node_modules/playwright-core/browsers.json'`. یعنی مقصر، کامیت `556ea8b` (پیاده‌سازی `page_automation` برای دکاپست، **قبل از** چاپار) بود، نه چاپار. `page-automation-provider.ts` یک `import { chromium } from "playwright"` **ثابت در سطح ماژول** داشت؛ چون خودِ ماژول `playwright-core` روی build استاندالون production کامل کپی نشده بود، همین import throw می‌کرد و چون `engine.ts` (که همه‌ی مسیرهای قیمت‌گذاری import می‌کنند) بدون قید‌وشرط `new PageAutomationProvider()` می‌سازد، کل چرخه‌ی ثبت سفارش (پستی + پیک موتوری) از کار می‌افتاد — کاملاً مستقل از این‌که شرکت دکاپست در دیتابیس باشد یا نه.

سه اصلاح انجام شد:
1. `page-automation-provider.ts`: `import` ثابت به `await import("playwright")` **پویا و داخل try/catch** تبدیل شد — دیگر هیچ خطای بارگذاری این ماژول نمی‌تواند به `engine.ts` سرایت کند.
2. کلید `PAGE_AUTOMATION_ENABLED = false` اضافه شد — `getQuote()` قبل از هر کاری این را چک می‌کند و بدون لمس Playwright/دیتابیس فوراً `available:false` برمی‌گرداند.
3. `engine.ts`: تابع مشترک `safeGetQuote()` اضافه شد — هر فراخوانی `provider.getQuote()` (برای همه‌ی providerها) try/catch می‌شود؛ از این به بعد throw هر provider فقط همان شرکت را از نتایج حذف می‌کند، نه کل درخواست را.
4. `CompanyForm.tsx`: گزینه‌ی «ربات استعلام خودکار» از دراپ‌داون «نحوه دریافت قیمت» حذف شد تا اصلاً قابل‌انتخاب نباشد.

فایل‌های چاپار که در ردیف قبلی revert شدند، عمداً دوباره اضافه نشدند (بخشی از این تسک نبودند و ربطی به علت خرابی نداشتند).

جزئیات کامل در بخش ⚠️۵ همین سند. بدون migration.

**کامیت:** `c2c26e5`

### ۱۴۰۵/۰۵/۱۲ (۲۰۲۶-۰۸-۰۱) — بازپیاده‌سازی چاپار با تست مقاومت در برابر خطای شبکه

اتصال چاپار (`src/lib/chapar/`, `external-api-provider.ts`, `trackOrderAction`, صفحه‌ی `/tracking`) دوباره پیاده‌سازی شد — این‌بار طبق درس حادثه‌ی بخش ⚠️۵: فقط `fetch` استفاده شد (بدون هیچ وابستگی خارجی سنگین مثل Playwright که باعث حادثه‌ی قبلی شد)، و دو لایه try/catch گذاشته شد (هم داخل `ExternalApiProvider`/`trackOrderAction`، هم `safeGetQuote` در `engine.ts`).

قبل از push، با mock کردن `global.fetch` برای شبیه‌سازی خطای شبکه‌ی واقعی (`ECONNREFUSED`، تایم‌اوت)، تایید شد که خطای چاپار به‌صورت `Error` عادی و قابل‌catch throw می‌شود (نه crash)، و در یک batch شبیه‌سازی‌شده‌ی چند شرکت، `Promise.all` هیچ‌وقت reject نشد — شرکت چاپار حذف شد، شرکت‌های دیگر نتیجه‌شان را برگرداندند.

⚠️ هنوز با API واقعی چاپار تست نشده (دسترسی شبکه‌ی sandbox به `app.krch.ir` مسدود است، مثل قبل). جزئیات کامل در بخش ⚠️۶.

بدون migration.

**کامیت:** `c1d4845`

### ۱۴۰۵/۰۵/۱۲ (۲۰۲۶-۰۸-۰۱) — لاگ موقت تشخیصی برای چاپار

بعد از دیپلوی نسخه‌ی امن‌شده‌ی چاپار، کاربر گزارش داد چاپار توی نتایج مقایسه قیمت ظاهر نمی‌شود — یعنی جایی در `ExternalApiProvider`/`resolveChaparCityCode` بی‌سروصدا `available:false`/`null` برمی‌گرداند، بدون این‌که علتش در لاگ Liara قابل‌دیدن باشد. سه نقطه‌ی «سکوت» زیر `console.error` گرفتند (رفتار safe/fallback هیچ‌جا تغییر نکرد، فقط لاگ اضافه شد):

1. `external-api-provider.ts` — catch اصلی: پیام خطا + stack.
2. `external-api-provider.ts` — وقتی `resolveChaparCityCode` برای مبدا/مقصد `null` برمی‌گرداند.
3. `external-api-provider.ts` — وقتی `getChaparQuote` پاسخ معتبر (`order.quote` عددی) برنمی‌گرداند؛ payload ارسالی هم لاگ می‌شود.
4. `city-map.ts` — catch داخلی `resolveChaparCityCode` که قبلاً کاملاً بی‌صدا بود (`catch { return null; }`)، حالا پیام خطای واقعی `get_state`/`get_city` را لاگ می‌کند.

همه‌ی لاگ‌ها با پیشوند `[Chapar]` تگ شده‌اند تا در لاگ Liara راحت grep شوند، و با کامنت `TODO(لاگ موقت تشخیصی)` علامت‌گذاری شده‌اند — بعد از پیدا شدن علت واقعی باید حذف/کم شوند (مثل الگوی قبلی `/api/debug` و حساب‌های تست تشخیصی). credentials (یوزرنیم/رمز) در هیچ‌کدام لاگ نمی‌شوند.

بدون migration — فقط `console.error`، هیچ تغییری در رفتار برگشتی توابع.

**کامیت:** `1ca79aa`

### ۱۴۰۵/۰۵/۱۲ (۲۰۲۶-۰۸-۰۱) — لاگ خام get_state/get_city + مقاوم‌سازی matching نام شهر/استان

لاگ `[Chapar]` تایید کرد که `get_state`/`get_city` بدون خطا جواب می‌دهند، ولی matching نام استان/شهر بادرو با نام چاپار شکست می‌خورد — یعنی مشکل شبکه/احراز هویت نیست، فرمت رشته‌ی اسم‌هاست. دو کار انجام شد:

1. **لاگ خام:** `city-map.ts` حالا وقتی matching استان یا شهر شکست بخورد، لیست خام اسم‌هایی که همان لحظه از `get_state`/`get_city` (برای همان استان) برگشته را کامل در لاگ چاپ می‌کند (`rawStatesFromChapar`/`rawCitiesFromChapar`) — تا فرمت واقعی چاپار (با/بدون پیشوند «استان»، فاصله‌گذاری، و…) مستقیماً دیده شود.
2. **`normalize()` مقاوم‌تر شد:** علاوه بر یکسان‌سازی قبلی (ي→ی، ك→ک، فاصله‌های تکراری)، حالا نیم‌فاصله/NBSP/zero-width space هم به فاصله‌ی معمولی تبدیل می‌شوند، و پیشوندهای رایج «استان »، «شهرستان »، «شهر » (که ممکن است فقط سمت چاپار باشند، نه سمت بادرو) حذف می‌شوند. با تست محلی (mock کردن `get_state`/`get_city`) تایید شد که مثلاً «استان تهران»/«شهر تهران» از سمت چاپار حالا با «تهران» بادرو درست match می‌شود؛ و برای یک mismatch واقعی (اسم کاملاً متفاوت)، هم لاگ خام درست چاپ می‌شود هم رفتار safe (`null`) حفظ می‌شود.

⚠️ اگر بعد از دیپلوی همچنان مشکل بود، لاگ `rawStatesFromChapar`/`rawCitiesFromChapar` باید دقیقاً نشان بدهد فرمت واقعی اسم‌های چاپار چیست تا `normalize()` بر همان اساس اصلاح شود (این تابع کاملاً local و بدون migration قابل‌تغییر است).

بدون migration.

**کامیت:** `0d4bf43`

### ۱۴۰۵/۰۵/۱۲ (۲۰۲۶-۰۸-۰۱) — لاگ HTTP status + بدنه‌ی خام پاسخ چاپار (تشخیص علت `rawStatesFromChapar: []`)

لاگ قبلی نشان داد `rawStatesFromChapar` همیشه `[]` است — یعنی خودِ `chaparRequest` هیچ خطایی throw نمی‌کرد (وگرنه لاگ catch جداگانه دیده می‌شد)، پس یا پاسخ HTTP خطا بود که به اشتباه به `[]` تفسیر می‌شد، یا پاسخ موفق با shape غیرمنتظره بود (`getChaparStates` فقط `data.states`/`data.data` را می‌شناسد؛ هر shape دیگری بی‌صدا `[]` می‌شود).

`chaparRequest()` در `client.ts` بازنویسی شد: به‌جای `res.json()` مستقیم (که اگر بدنه JSON معتبر نباشد خودش throw می‌کند و بدنه‌ی خام هیچ‌وقت دیده نمی‌شود)، حالا اول `res.text()` خوانده و لاگ می‌شود، بعد parse. لاگ `[Chapar] پاسخ خام HTTP` برای **هر** درخواست به چاپار (get_quote، tracking، get_state، get_city) این‌ها را نشان می‌دهد: مسیر، payload دقیق ارسالی (رمز عبور `***REDACTED***`)، HTTP status/statusText، و کل بدنه‌ی خام پاسخ. اگر status خطا باشد throw می‌کند (پیام خطا هم شامل بخشی از بدنه است)؛ اگر بدنه JSON نامعتبر باشد (مثلاً صفحه‌ی خطای HTML) جداگانه throw می‌کند؛ اگر JSON معتبر ولی shape غیرمنتظره باشد، رفتار قبلی (`[]`) حفظ می‌شود ولی حالا بدنه‌ی خام واقعی در لاگ بالا کاملاً دیده می‌شود.

با تست محلی (mock کردن `fetch`) هر سه سناریو تایید شد: خطای HTTP ۴۰۱ (status+body در لاگ + throw)، بدنه‌ی غیر-JSON (raw text در لاگ + throw جداگانه)، و JSON معتبر با shape غیرمنتظره (raw body کامل در لاگ + `[]` بدون throw). credentials هیچ‌جا لاگ نمی‌شوند.

بدون migration.

**کامیت:** `26e5fd9`

### ۱۴۰۵/۰۵/۱۲ (۲۰۲۶-۰۸-۰۱) — رفع باگ: نتیجه‌ی خالی چاپار برای ۶ ساعت کش می‌شد (لاگ جدید هم به همین دلیل دیده نمی‌شد)

بعد از دیپلوی لاگ خام HTTP (کامیت `26e5fd9`)، کاربر گزارش داد آن لاگ اصلاً روی production ظاهر نمی‌شود — فقط همان لاگ قدیمی‌تر (`rawStatesFromChapar: []`) دیده می‌شود. فرضیه‌ی درست کاربر: کش درون‌حافظه‌ای نگاشت شهر (`stateCache`/`cityCache` در `city-map.ts`) نتیجه‌ی **خالی** `get_state`/`get_city` را هم — دقیقاً مثل یک نتیجه‌ی موفق — با TTL ۶ ساعته کش می‌کرد. یعنی اولین باری که `get_state` (به هر دلیلی، حتی گذرا) لیست خالی برگرداند، آن `[]` برای ۶ ساعت کش شد؛ هر درخواست بعدی (از جمله تست‌های کاربر بعد از دیپلوی جدید) فقط از همان کش خالی می‌خواند و **اصلاً دوباره واقعاً به چاپار درخواست نمی‌زد** — پس کد جدید لاگ‌دار (`chaparRequest`) هیچ‌وقت دوباره اجرا نمی‌شد.

رفع شد: `getCachedStates`/`getCachedCityMap` حالا فقط وقتی نتیجه غیرخالی باشد آن را کش می‌کنند؛ نتیجه‌ی خالی/خطا هرگز کش نمی‌شود — یعنی هر درخواست بعدی دوباره واقعاً `get_state`/`get_city` را صدا می‌زند تا یک نتیجه‌ی موفق واقعی به دست بیاید (و لاگ خام HTTP هر بار دوباره چاپ می‌شود). با تست محلی تایید شد: (۱) وقتی چاپار همیشه `[]` برمی‌گرداند، هر سه فراخوانی پشت‌سرهم واقعاً سه بار `fetch` واقعی زدند (نه یک‌بار با کش)، (۲) وقتی چاپار نتیجه‌ی موفق و غیرخالی برمی‌گرداند، کش طبق انتظار کار می‌کند (فقط یک‌بار `fetch`، بقیه از کش).

**⚠️ این هم یک باگ تشخیصی بود هم یک باگ واقعی production:** حتی مستقل از دیباگ فعلی، اگر یک روز `get_state` واقعی چاپار به‌خاطر یک خطای گذرا (مثلاً یک timeout یا 5xx موقت) یک نتیجه‌ی خالی برمی‌گرداند، طبق کد قبلی چاپار برای **۶ ساعت کامل** برای همه‌ی مشتری‌ها از دسترس خارج می‌ماند، حتی بعد از رفع مشکل چاپار — این رفتار دیگر وجود ندارد.

بدون migration.

**کامیت:** `744611b`

### ۱۴۰۵/۰۵/۱۲ (۲۰۲۶-۰۸-۰۱) — علت نهایی پیدا شد: شکل واقعی پاسخ get_state/get_city چاپار متفاوت بود — parse اصلاح شد

بعد از رفع باگ کش (ردیف قبلی)، لاگ خام HTTP بالاخره دائم دیده شد و علت واقعی مشخص شد: `get_state`/`get_city` چاپار همیشه HTTP ۲۰۰ با داده‌ی واقعی برمی‌گرداندند، ولی شکل پاسخ این بود:
```json
{ "result": true, "message": "", "objects": { "state": [ { "no": "...", "name": "..." }, ... ] } }
```
یعنی لیست داخل `objects.state` (و برای شهرها `objects.city`) بود، نه یک فیلد مستقیم `states`/`data` که کد قبلی دنبالش می‌گشت — و شناسه‌ی هر آیتم هم `no` بود، نه `id`. کد قبلی این شکل را نمی‌شناخت، همیشه `[]` می‌گرفت، و این [] هم (به‌خاطر باگ ردیف قبلی) کش می‌شد — دو باگ روی هم افتاده بودند.

`getChaparStates`/`getChaparCities` در `client.ts` اصلاح شدند: حالا `objects.state`/`objects.city` را هم به‌عنوان یک fallback می‌شناسند (علاوه بر `states`/`cities`/`data` قبلی که دست‌نخورده ماندند)، و شناسه‌ی هر آیتم از `id` یا `no` یا `code` (هرکدام موجود بود) گرفته می‌شود. **فقط همین دو تابع تغییر کردند** — هیچ فایل دیگری (`city-map.ts`, `external-api-provider.ts`, `engine.ts`, صفحات سفارش) لمس نشد.

با تست محلی (mock کردن `fetch`) تایید شد:
1. شکل واقعی (`objects.state`/`{no,name}`) درست parse می‌شود؛ `resolveChaparCityCode` سرتاسر («تهران»→«تهران») کد شهر درست را برمی‌گرداند.
2. یک شکل کاملاً غیرمنتظره (نه هیچ‌کدام از fallbackها) باعث `[]` می‌شود، نه throw/crash.
3. `objects` موجود ولی `objects.state` نبود → باز هم `[]`، نه crash.
4. آیتم‌هایی که `name` یا شناسه ندارند، فیلتر می‌شوند (بقیه‌ی آیتم‌های سالم لیست همچنان پردازش می‌شوند).

بدون migration.

**کامیت:** `0057881`

### ۱۴۰۵/۰۵/۱۳ (۲۰۲۶-۰۸-۰۱) — رفع weight/value نامعتبر ارسالی به get_quote چاپار + کشف باگ واحد ریال/تومان

بعد از این‌که parse چاپار درست شد، لاگ `get_quote` نشان داد `weight: 0.001` و `value: 0` فرستاده می‌شود — بررسی شد:

1. **تبدیل گرم→کیلوگرم خودش درست بود** (همیشه دقیقاً `/۱۰۰۰`). عدد `0.001` یعنی `weightGrams=1` رسیده بود — این با حداقل مجاز خودِ بادرو (`packageSchema`، ۱۰۰ گرم) در تناقض است، پس به‌احتمال زیاد یک سفارش واقعی کامل نبوده (مثلاً درخواست پیش‌نمایش زنده‌ی قیمت که در `ParcelDetailsStep.tsx` با هر تایپ کاربر با debounce ۵۰۰ms شلیک می‌شود، وسط تایپ عدد وزن). یک گارد اضافه شد: اگر `weightKg < 0.1` (دقیقاً همان حداقل `packageSchema`)، دیگر به چاپار فرستاده نمی‌شود — `available:false` با پیام «وزن هنوز کامل/معتبر نیست» برمی‌گردد.
2. **`value: 0`** واقعاً به‌خاطر این بود که فیلد «ارزش مرسوله» در بادرو اختیاری است و کاربر پر نکرده بود (نه باگ). چون مشخص نیست چاپار با `value=0` هم quote درست می‌دهد، حالا وقتی خالی/صفر باشد یک مقدار نمادین ثابت (۱۰۰,۰۰۰ ریال) فرستاده می‌شود. **این مقدار حدسی و تایید‌نشده است** — اگر چاپار حق‌بیمه را متناسب با value حساب کند، ممکن است روی مبلغ نهایی اثر بگذارد؛ نیاز به تایید با مستندات/پشتیبانی چاپار دارد.
3. **⚠️ باگ جانبی مهم‌تر کشف شد:** `Order.declaredValue` در بادرو بر حسب **تومان** ذخیره/جمع‌آوری می‌شود (برچسب فرم `ParcelDetailsStep.tsx`: «ارزش مرسوله (تومان، اختیاری)»)، ولی طبق مستندات ارائه‌شده، `value` در `get_quote` چاپار بر حسب **ریال** است. کد قبلی مقدار تومان را بدون تبدیل مستقیم می‌فرستاد — یعنی همیشه ارزش کالا را ۱۰ برابر کمتر از واقعی به چاپار اعلام می‌کرد. حالا همیشه در ۱۰ ضرب می‌شود.

هر سه اصلاح **فقط** در `external-api-provider.ts` انجام شد — هیچ فایل دیگری (`engine.ts`, `client.ts`, `city-map.ts`, صفحات سفارش) لمس نشد؛ یعنی مسیر عادی ثبت سفارش برای شرکت‌های `internal_formula`/`page_automation` کاملاً دست‌نخورده ماند.

با تست محلی (بدون نیاز به دیتابیس، چون منطق weight/value مستقل تست شد) شش سناریو تایید شد: سفارش کامل واقعی (۲۵۰۰ گرم/۵۰۰,۰۰۰ تومان → `weight:2.5`, `value:5,000,000` ریال)، همان سناریوی باگ (`weightGrams:1` → رد می‌شود، نه ارسال به چاپار)، `declaredValue` خالی (→ مقدار نمادین)، پاکت (→ همیشه ۰.۵ کیلوگرم)، دقیقاً مرز مجاز (۱۰۰ گرم → قبول)، و یک فراخوانی end-to-end واقعی `get_quote` با این مقادیر و شکل واقعی پاسخ چاپار.

بدون migration.

**کامیت:** `f51536f`

### ۱۴۰۵/۰۵/۱۳ (۲۰۲۶-۰۸-۰۱) — get_state/get_city موفق، ولی get_quote رد می‌کند: «امکان مقایسه آدرس ورودی از شهر مبدا و مقصد فراهم نشد» + لاگ دقیق‌تر

پیشرفت مهم: `get_state`/`get_city` هر دو با `result:true` موفق شدند و `weight`/`value` ارسالی هم بعد از اصلاح قبلی منطقی شدند. مشکل فعلی مرحله‌ی بعدی است — `get_quote` با `result:false` و پیام `«امکان مقایسه آدرس ورودی از شهر مبدا و مقصد فراهم نشد»` رد می‌کند. علت دقیق (کد `origin`/`destination` اشتباه است؟ `method` نامعتبر است؟ چیز دیگری لازم است؟) از این sandbox قابل تشخیص نیست (دسترسی شبکه به `app.krch.ir` مسدود است، طبق تمام دورهای قبلی)، پس فقط لاگ تشخیصی بیشتر اضافه شد تا کاربر با تست بعدی دقیق‌تر ببیند:

1. **`getChaparQuote` در `client.ts`:** وقتی پاسخ `quote` عددی معتبر نداشته باشد، حالا `sentOrigin`/`sentDestination`/`sentMethod`/`sentWeight`/`sentValue` را به‌صورت فیلدهای صریح و جدا (نه دفن‌شده داخل یک شیء تودرتو) لاگ می‌کند، به‌علاوه `chaparResult`/`chaparMessage` — یعنی پیام واقعی چاپار (مثل «امکان مقایسه...») حالا با یک برچسب مشخص (`chaparMessage`) قابل‌دیدن است، نه فقط داخل `rawResponseBody` خام.
2. **fallback اضافه برای شکل پاسخ موفق:** چون `get_state`/`get_city` نشان دادند چاپار داده را داخل `objects` می‌گذارد، احتمال داده شد `quote` هم ممکن است در `objects.quote`/`objects.order.quote` باشد — این‌ها هم به‌عنوان fallback اضافه شدند (علاوه بر `order.quote` قبلی که دست‌نخورده ماند).
3. **`external-api-provider.ts`:** لاگ تکراری قبلی (که همان `origin`/`destination` را دوباره در یک شیء دیگر چاپ می‌کرد) ساده شد تا فقط `companyId` را برای ارتباط‌دادن به لاگ دقیق‌تر بالا اضافه کند — دو لاگ همپوشان نبود.

⚠️ درباره‌ی سؤال «آیا کد شهر (`no` از `get_city`) درست است یا فیلد دیگری لازم است؟» — این کد **بدون تایید مستندات رسمی چاپار** همچنان همان `no` را می‌فرستد (بدون تغییر، چون دستور صریح بود فقط لاگ اضافه شود، نه رفتار). با پیام دقیق `chaparMessage` که این‌بار قابل‌دیدن است، دور بعد تست می‌شود که آیا مشکل واقعاً کد شهر است یا `method`.

با تست محلی (mock کردن `fetch`) تایید شد: سناریوی دقیق گزارش‌شده (`result:false` + همین پیام) حالا لاگ کامل با `sentOrigin`/`sentDestination`/`chaparMessage` می‌دهد و `null` برمی‌گرداند (نه crash)؛ مسیر موفق قبلی (`order.quote`) دست‌نخورده ماند؛ fallback جدید (`objects.quote`) هم کار می‌کند.

بدون migration.

**کامیت:** `2620d39`

### ۱۴۰۵/۰۵/۱۳ (۲۰۲۶-۰۸-۰۱) — لاگ تایید صحت match شهر (هم‌نام‌های احتمالی) + تست خودکار چند method

پیام خطای «امکان جمع‌آوری از شهر مبدا وجود ندارد» برای تهران غیرمنطقی بود (تهران باید پوشش داشته باشد). دو نگرانی مطرح شد: ممکن است کد شهرِ match‌شده واقعاً مال یک هم‌نام در استان دیگر باشد (نه تهرانِ اصلی)، یا کد `method` نامعتبر باشد. دو اصلاح تشخیصی انجام شد:

1. **`resolveChaparCityCode` در `city-map.ts`** حالا به‌جای فقط یک `string` (کد)، یک شیء کامل برمی‌گرداند: `{ code, matchedCityName, matchedStateId, matchedStateName }` — یعنی اسم دقیق شهر و استانی که چاپار واقعاً match کرده هم در دسترس است، نه فقط کد عددی. `external-api-provider.ts` این اطلاعات را قبل از فراخوانی `get_quote` در یک لاگ جدید (`[Chapar] نگاشت شهر مبدا/مقصد به کد چاپار`) کامل چاپ می‌کند — با تست محلی تایید شد که اگر یک هم‌نام تصادفی در استان اشتباه match شود (مثلاً «تهران» در استان «اصفهان»)، این لاگ دقیقاً همین ناهماهنگی را نشان می‌دهد.
2. **تست خودکار چند `method`:** به‌جای هاردکد `method:"1"`، حالا پنج کد محتمل (طبق پیشنهاد کاربر: ۱=زمینی، ۶=هوایی، ۱۱=پستی، ۳۵=چاپار پلاس، ۹۷=پاکت) به‌ترتیب امتحان می‌شوند؛ هرکدام جواب داد (یک `quote` عددی برگرداند)، همان استفاده می‌شود و بقیه امتحان نمی‌شوند (لاگ `[Chapar] method کارآمد پیدا شد`). این یک تغییر رفتاری موقت و تشخیصی است — بعد از پیدا شدن `method` درست، باید با همان یک مقدار ثابت جایگزین شود (کامنت `TODO` در کد این را یادآوری می‌کند).

⚠️ **ریسک احتمالی این تست:** فرض شد `get_quote` یک endpoint فقط-خواندنی (بدون اثر جانبی مثل رزرو/ثبت سفارش) است — طبق نام‌گذاری منطقی به نظر می‌رسد، ولی از مستندات رسمی تایید نشده. اگر بعد از این تست چیزی غیرمنتظره (مثل سفارش ناخواسته) دیده شد، باید فوراً اطلاع داده شود.

با تست محلی ۴ سناریو تایید شد: نگاشت موفق با اطلاعات کامل، سناریوی هشداری هم‌نام در استان اشتباه (لاگ درست نشانش می‌دهد)، حلقه‌ی method که دقیقاً تا اولین جواب مثبت امتحان می‌کند و متوقف می‌شود (بدون امتحان بی‌مورد بقیه)، و سناریوی هیچ‌کدام‌جواب‌نده (بدون crash، `null` امن).

بدون migration.

**کامیت:** `aa329af`

### ۱۴۰۵/۰۵/۱۴ (۲۰۲۶-۰۸-۰۱) — get_quote از صفر با فرمت واقعی تایید‌شده‌ی پشتیبانی چاپار بازنویسی شد + پاک‌سازی کدهای موقت دیباگ

پشتیبانی چاپار تایید کرد `sender_code`/`receiver_code = "1000"` مقدار عمومی/پیش‌فرض درستی است، و یک نمونه‌ی `curl` واقعی داد که نشان می‌دهد `get_quote` **کاملاً متفاوت** از بقیه‌ی endpointها کار می‌کند — نه یوزرنیم/رمز در بدنه‌ی JSON، بلکه:
- احراز هویت با هدر ثابت `APP-AUTH` (یک مقدار ثابت سطح‌اپلیکیشن، نه credential حساب بادرو — هاردکد است).
- بدنه `multipart/form-data` با یک فیلد `input` که مقدارش خودِ رشته‌ی `JSON.stringify` شده‌ی `{ order: {...} }` است (نه JSON خام).
- فیلدهای `order`: `origin`, `destination`, `weight`, `value`, `method`, `sender_code:"1000"`, `receiver_code:"1000"`, `cod:"0"` — همه به‌صورت رشته.

**پاک‌سازی (فقط چیزهای قطعاً مربوط به دیباگ `get_quote`):**
- حلقه‌ی تست ۵ کد `method` (۱/۶/۱۱/۳۵/۹۷) در `external-api-provider.ts` کاملاً حذف شد؛ به یک فراخوانی تک با `method:"1"` (همان مقدار نمونه‌ی پشتیبانی) برگشت.
- fallback حدسی `objects.quote`/`objects.order.quote` در `client.ts` حذف شد (چون `get_quote` حالا کاملاً از صفر با فرمت واقعی بازنویسی شده، این حدس دیگر موضوعیت نداشت).
- لاگ پیچیده‌ی قبلی `get_quote` (با `sentOrigin`/`sentMethod`/... جدا) با یک لاگ ساده‌تر جایگزین شد که فقط وقتی `quote` عددی معتبر برنگردد فعال می‌شود و `sentOrder` کامل + `chaparResult`/`chaparMessage` را نشان می‌دهد.

**چیزهایی که عمداً دست نخوردند** (طبق دستور صریح): `chaparRequest()` (استفاده‌شده توسط `get_state`/`get_city`/`tracking`)، `getChaparStates`/`getChaparCities`/`getChaparTracking`، `city-map.ts` (نگاشت شهر و لاگ‌های آن)، منطق تبدیل وزن/ارزش (گرم→کیلوگرم، تومان→ریال) در `external-api-provider.ts`، لاگ تایید صحت match شهر (`[Chapar] نگاشت شهر مبدا/مقصد به کد چاپار`) — چون این‌ها ربطی به دیباگ خاص `get_quote` نداشتند یا هنوز سؤال بازشان حل نشده بود، `safeGetQuote()` در `engine.ts`، و `page-automation-provider.ts`/هیچ شرکت دیگری.

با تست محلی (mock کردن `fetch`) تایید شد: درخواست ارسالی **دقیقاً** با نمونه‌ی `curl` پشتیبانی چاپار یکی است (URL، هدر `APP-AUTH`، نوع بدنه `FormData`، و تک‌تک فیلدهای `order` به‌صورت رشته) — با ۱۳ چک جداگانه؛ سناریوی `result:false` با پیام خطا هنوز safe و بدون crash است (`null`)؛ خطای HTTP واقعی (۵۰۰) هنوز `throw` می‌شود تا `safeGetQuote`/try-catch بالادستی آن را بگیرد (بدون رگرسیون در مسیر ایمنی).

بدون migration.

**کامیت:** `9b4340a`

### ۱۴۰۵/۰۵/۱۴ (۲۰۲۶-۰۸-۰۱) — get_quote موفق می‌شود ولی قیمت استخراج نمی‌شد؛ استخراج چندمسیره + لاگ خام دائمی

بعد از بازنویسی get_quote، لاگ نشان داد چاپار واقعاً موفق می‌شود (`result:true, message:''`) ولی کد قدیم فقط دنبال `order.quote` می‌گشت و چیزی پیدا نمی‌کرد — یعنی قیمت جای دیگری در پاسخ است، احتمالاً (طبق الگوی `get_state`/`get_city`) داخل یک لایه‌ی `objects` تودرتو.

دو اصلاح هم‌زمان در `src/lib/chapar/client.ts` (فقط همین فایل):

1. **لاگ خام دائمی (نه موقت):** بدنه‌ی کامل خام پاسخ get_quote (`rawResponseBody`) و `httpStatus` حالا **همیشه** لاگ می‌شوند، نه فقط موقع شکست — چون هزینه‌ای ندارد و برای هر بررسی بعدی بدون نیاز به دیپلوی جدید در دسترس می‌ماند (این یکی، برخلاف بقیه‌ی لاگ‌های `TODO`دار قبلی، عمداً بدون برچسب حذف‌شدنی است).
2. **استخراج قیمت چندمسیره:** به‌جای فقط `order.quote`، حالا چهار مسیر به‌ترتیب اولویت امتحان می‌شوند: `order.quote` → `objects.order.quote` → `objects.quote` → `quote` (سطح بالا). هرکدام هم به‌صورت عدد هم رشته‌ی عددی (چون چاپار در درخواست همه‌چیز را رشته می‌فرستد، احتمال رشته‌بودن در پاسخ هم منطقی بود) پذیرفته می‌شود.

با تست محلی (mock کردن `fetch`) ۸ سناریو تایید شد: هر چهار مسیر (هم به‌صورت عدد هم رشته برای دو مسیر از آن‌ها)، اولویت‌بندی درست وقتی چند مسیر هم‌زمان مقدار دارند (`order.quote` برنده می‌شود)، و دقیقاً سناریوی واقعی گزارش‌شده (`result:true` بدون هیچ مسیر شناخته‌شده) که باید همچنان `null` امن بدهد (نه crash) — همه ✅.

بدون migration.

**کامیت:** `f1a5186`

### ۱۴۰۵/۰۵/۱۴ (۲۰۲۶-۰۸-۰۱) — رفع باگ واحد پول: قیمت چاپار ریال بود، بادرو ۱۰ برابر بیشتر نشان می‌داد

با مقایسه‌ی مستقیم با ماشین‌حساب رسمی چاپار (کرج→قم، وزن ۱ کیلوگرم، ارزش ۱۰,۰۰۰,۰۰۰ ریال) تایید شد: چاپار عدد `۱,۵۵۷,۱۶۰ ریال` داد، ولی بادرو همان عدد را بدون تبدیل به‌عنوان **تومان** نشان می‌داد — یعنی قیمت نمایش‌داده‌شده دقیقاً ۱۰ برابر واقعی بود.

در `external-api-provider.ts` (تنها فایل تغییریافته)، بلافاصله بعد از دریافت `quote` از `getChaparQuote` و **قبل از** ساخت خروجی نهایی (`price`/`breakdown`)، `quote.total` و تک‌تک مقادیر `quote.costs` بر ۱۰ تقسیم می‌شوند (ریال → تومان) — این تبدیل فقط روی **خروجیِ** همین provider اعمال می‌شود؛ منطق ورودی (تبدیل تومان→ریال برای `value` ارسالی، تبدیل گرم→کیلوگرم برای `weight`)، بقیه‌ی providerها (`internal_formula`, `page_automation`)، و `engine.ts`/`safeGetQuote` کاملاً دست‌نخورده ماندند.

با تست محلی ۴ سناریو تایید شد: دقیقاً همان نمونه‌ی واقعی گزارش‌شده (`۱,۵۵۷,۱۶۰` ریال → `۱۵۵,۷۱۶` تومان)، تبدیل تک‌تک اجزای `breakdown`/`costs` (نه فقط قیمت کل)، حالت بدون `costs` (fallback هم عدد تبدیل‌شده را نشان می‌دهد)، و گردکردن صحیح برای اعداد غیرمضرب ۱۰.

بدون migration.

**کامیت:** `5c4238c`

### ۱۴۰۵/۰۵/۱۴ (۲۰۲۶-۰۸-۰۱) — سه اسلاید جدید هدر صفحه اصلی (طرح کامل، بدون متن جداگانه روی تصویر)

سه فایل تصویر جدید (۱۰۲۴×۲۵۶، طرح کامل با متن فارسی پخته‌شده داخل خودِ تصویر) جایگزین سه اسلاید قبلی شدند:

- **فایل‌ها:** `public/slides/slide-1-intro.png`, `slide-2-price.png`, `slide-3-postal-flight.png` حذف شدند؛ `public/slides/slide1.jpg` (مقایسه قیمت در چند ثانیه)، `slide2.jpg` (ارسال به تمام نقاط ایران)، `slide3.jpg` (ارسال درون‌شهری) جایگزین شدند (همین ترتیب = `orderIndex` 0/1/2).
- **`prisma/seed.ts`:** آدرس تصویر سه اسلاید پیش‌فرض به فایل‌های جدید آپدیت شد؛ `description` حذف شد (چون دیگر جایی نمایش داده نمی‌شود).
- **`HomeSlider.tsx`:** چون تصاویر جدید طرح کامل و آماده‌اند (متن از قبل داخل خودِ تصویر است)، لایه‌های قبلی که مخصوص تصاویر ساده/پس‌زمینه‌ای بودند حذف شدند — گرادیان رنگی ترکیبی (`backgroundBlendMode`)، اسکریم تیره‌ی نیمه‌شفاف، و بلوک `<h2>`/`<p>` عنوان و توضیح. حالا فقط خودِ تصویر تمیز نمایش داده می‌شود؛ عنوان اسلاید همچنان به‌عنوان `aria-label`/`role="img"` برای screen reader نگه داشته شده (خوانایی حفظ شد، فقط بصری نیست).

**⚠️ نکته‌ی حیاتیِ همیشگی این پروژه دوباره صدق می‌کند:** `prisma db seed` روی production خودکار اجرا نمی‌شود (بخش ⚠️۱)، پس تغییر `seed.ts` **به‌تنهایی روی دیتابیس واقعی production اثر نمی‌گذارد** — سه ردیف قبلی `HomepageSlide` در دیتابیس زنده هنوز به فایل‌های حذف‌شده اشاره می‌کنند تا وقتی کسی دستی آن‌ها را به‌روز کند. طبق تصمیم کاربر، این کار **از پنل ادمین** (`/admin/content` → بخش «اسلایدهای صفحه اصلی») انجام می‌شود، نه با اسکریپت دستی روی Liara Shell: سه اسلاید قدیمی حذف (دکمه «حذف» کنار هرکدام)، سه اسلاید جدید با فرم بالای همان بخش اضافه شوند — «آدرس تصویر»: `/slides/slide1.jpg` (و به همین ترتیب ۲ و ۳)، «عنوان»: هر متنی دلخواه (چون دیگر نمایش داده نمی‌شود، فقط برای دسترس‌پذیری)، «لینک» خالی، «ترتیب»: ۰، ۱، ۲.

با تست محلی (رندر واقعی کامپوننت با `renderToStaticMarkup`، بدون نیاز به دیتابیس/دیپلوی) تایید شد: هر سه فایل جدید در HTML خروجی هست، هیچ فایل قدیمی نیست، `aria-label` با عنوان درست ست می‌شود، دیگر هیچ `<h2>`/متن بصری یا گرادیان/اسکریم تیره‌ی قبلی رندر نمی‌شود.

بدون migration.

**کامیت:** `ff29e4e`

### ۱۴۰۵/۰۵/۱۴ (۲۰۲۶-۰۸-۰۱) — اسلاید سوم با نسبت متفاوت (۲.۷۶:۱) جایگزین شد؛ `HomeSlider` به `contain` تغییر کرد تا هیچ‌وقت محتوا نبرد

فایل `public/slides/slide3.jpg` با یک طرح جدید (۱۰۸۰×۳۹۱، نسبت ≈۲.۷۶:۱ — برخلاف دو اسلاید دیگر که ≈۴:۱ هستند) جایگزین شد. چون `HomeSlider.tsx` قبلاً از `backgroundSize: cover` استفاده می‌کرد (که همیشه تصویر را برای پرکردن کامل کادر می‌برد)، این اسلاید با نسبت متفاوت قطعاً از بالا/پایین بریده می‌شد.

**راه‌حل عمومی به‌جای اختصاصی برای همین یک اسلاید:** به‌جای اضافه‌کردن یک استثنای مخصوص این فایل (که هم شکننده است هم برای هر اسلاید دیگری با نسبت متفاوت در آینده دوباره باید دستی تکرار شود)، `backgroundSize` برای **همه‌ی** اسلایدها از `cover` به `contain` تغییر کرد — یعنی هیچ‌وقت، برای هیچ نسبت تصویری، چیزی از محتوای اسلاید بریده نمی‌شود. چون `contain` ممکن است فضای خالی (letterbox) دور تصویر بگذارد، دو نکته اضافه شد: `backgroundRepeat: "no-repeat"` (وگرنه فضای خالی با تکرار/کاشی‌شدن تصویر پر می‌شد — رفتار پیش‌فرض CSS برای background) و رنگ پس‌زمینه‌ی `bg-brand-blue-50` (آبی خیلی روشن، هم‌خانواده‌ی رنگ آسمانِ خودِ طرح‌ها) تا فضای خالی احتمالی محسوس/زننده نباشد.

این یک تغییر کاملاً CSS/کامپوننتی بود — **هیچ نیازی به migration یا تغییر schema نبود** (هیچ‌جا نسبت تصویر در دیتابیس ذخیره نمی‌شود؛ `contain` در سطح مرورگر خودش با نسبت واقعی هر فایل تصویر کنار می‌آید).

⚠️ طبق همان نکته‌ی همیشگی، دیتابیس زنده تا وقتی از پنل `/admin/content` به‌روزرسانی نشود (طبق راهنمای ردیف قبلی همین تاریخچه)، اسلاید سوم را اصلاً نمایش نمی‌دهد — این تغییر مستقل از آن مشکل است و فقط نحوه‌ی *نمایش* تصویر (بعد از این‌که دیتابیس درست شد) را اصلاح می‌کند.

با تست محلی (`renderToStaticMarkup`) تایید شد: `backgroundSize:contain` و `backgroundRepeat:no-repeat` و کلاس `bg-brand-blue-50` روی هر سه اسلاید اعمال شده، اسلاید سوم به فایل جدید اشاره می‌کند، و دیگر هیچ‌جا `cover` باقی نمانده.

بدون migration.

**کامیت:** `ea8fb44`

### ۱۴۰۵/۰۵/۱۴ (۲۰۲۶-۰۸-۰۱) — اسلاید سوم اصلاح شد (خط دوم متن حذف)، دوباره هم‌نسبت با بقیه؛ `HomeSlider` به `cover` برگشت

نسخه‌ی اصلاح‌شده‌ی `public/slides/slide3.jpg` جایگزین شد — خط دوم متن ریز پایین طرح (که قبلاً بریده می‌شد) حذف شده، فقط خط اول و آیکون مانده‌اند. ابعاد این نسخه دقیقاً `۱۰۲۴×۲۵۶` (۴:۱) است — یعنی دیگر با دو اسلاید دیگر هم‌نسبت است، برخلاف نسخه‌ی قبلی (۲.۷۶:۱) که باعث اصلاح موقت `contain` در ردیف قبلی همین تاریخچه شده بود.

چون علت اصلی (نسبت متفاوت) دیگر وجود ندارد، `HomeSlider.tsx` به حالت ساده‌ی قبلی برگشت: `backgroundSize: cover` (به‌جای `contain`)، بدون نیاز به `backgroundRepeat`/رنگ letterbox (`bg-brand-blue-50`) — همه حذف شدند چون دیگر موضوعیتی ندارند. کامنت کد هم توضیح می‌دهد که اگر در آینده اسلایدی با نسبت متفاوت اضافه شد، همان راه‌حل `contain` (که در تاریخچه‌ی این فایل مستند است) باید دوباره به کار گرفته شود.

این یک تغییر کاملاً فایل تصویر + CSS بود — **بدون نیاز به migration/schema** (مثل ردیف قبلی، هیچ‌جا نسبت تصویر در دیتابیس ذخیره نمی‌شود).

با تست محلی (`renderToStaticMarkup`) تایید شد: `backgroundSize:cover` روی هر ۳ اسلاید برگشته، `contain`/`backgroundRepeat`/`bg-brand-blue-50` دیگر هیچ‌کجا نیستند، اسلاید سوم به فایل جدید اشاره می‌کند و `aria-label` درست است.

⚠️ همان یادآوری همیشگی: دیتابیس زنده تا وقتی از پنل `/admin/content` به‌روز نشود، اسلایدهای جدید را اصلاً نمایش نمی‌دهد — این تغییر مستقل از آن است.

بدون migration.

**کامیت:** `793c264`

### ۱۴۰۵/۰۵/۱۵ (۲۰۲۶-۰۸-۰۳) — صفحات لاگین ادمین/شرکت دیگر با سشن معتبر خودکار به داشبورد ریدایرکت نمی‌کنند

قبلاً `src/app/admin/login/page.tsx` و `src/app/company/login/page.tsx` هردو، **قبل از** رندر فرم، سشن موجود را چک می‌کردند (`getStaffSession()`/`getCompanySession()`) و اگر کوکی سشن معتبر بود (که تا ۳۰ روز طول عمر دارد — `maxAge: 60*60*24*30` در `session.ts`)، بی‌سروصدا و بدون نمایش فرم به داشبورد `redirect` می‌کردند. این عمدی بود (الگوی رایج «اگه لاگین کردی دیگه فرم نبین»)، ولی رفتار مطلوب این بود که صفحه‌ی لاگین همیشه، بدون قید‌وشرط، فرم را نشان دهد.

هر دو فایل به یک شکل اصلاح شدند: بلوک چک سشن + `redirect("/admin")`/`redirect("/company")` کامل حذف شد؛ importهای مربوطه (`redirect` از `next/navigation`، `getStaffSession`/`getCompanySession` از `@/lib/auth/session`) هم چون دیگر جای دیگری در همان فایل استفاده نمی‌شدند حذف شدند؛ چون دیگر هیچ `await` ای در بدنه نبود، `async` هم از امضای تابع کامپوننت برداشته شد.

**هیچ‌چیز دیگری تغییر نکرد** (طبق دستور صریح): `proxy.ts` (که مسیر عکس را انجام می‌دهد — کاربر بدون سشن معتبر را از داشبورد به لاگین می‌فرستد) دست‌نخورده ماند؛ `staffLoginAction`/`companyLoginAction` در `src/actions/auth.ts` هم دست‌نخورده ماندند — این دو تابع همیشه یوزرنیم/رمز را مستقل از هر سشن قبلی مستقیم از دیتابیس چک می‌کنند و یک سشن تازه می‌سازند، پس هیچ وابستگی‌ای به کد حذف‌شده نداشتند.

بدون migration — این فقط حذف چند خط منطق frontend بود (هیچ‌جا داده‌ی دیتابیس یا schema درگیر نبود).

**کامیت:** (بعد از این commit ثبت می‌شود)

---

*این فایل به‌صورت خودکار توسط دستیار پس از هر تغییر مهم به‌روزرسانی می‌شود.*
