# مستندات پروژه بادرو (Badro)

> این فایل مرجع داخلی وضعیت پروژه است — بعد از هر تغییر مهم باید به‌روزرسانی شود (بخش «تاریخچه‌ی تغییرات» در پایین).
> **هیچ رمز عبور یا API Key واقعی در این فایل نوشته نمی‌شود** — فقط اسم متغیرهای محیطی و نمونه‌رمزهای تست ذکر می‌شود.
> این نسخه یک بازنویسی کامل است (۲۰۲۶-۰۷-۳۰) تا هر مهندس یا نشست جدید Claude Code بدون نیاز به خواندن کل تاریخچه‌ی مکالمات قبلی، بتواند سریع context بگیرد.

---

## ⚠️ نکات حیاتی — قبل از هر کاری این بخش را کامل بخوانید

### ۱. `scripts/start.sh` روی دیپلوی فعلی هیچ‌وقت اجرا نمی‌شود

محیط دیپلوی فعلی **Liara** است، با `output: standalone` در `next.config.ts`. پلتفرم برای اپ‌های Next.js با خروجی standalone، مستقیماً فایل `server.js` تولیدشده را اجرا می‌کند — **نه** دستور `"start": "sh scripts/start.sh"` که در `package.json` تعریف شده را.

پیامد این موضوع: دو مرحله‌ای که قرار بود طبق `scripts/start.sh` **خودکار و قبل از بالا آمدن هر بار سرور** اجرا شوند، هیچ‌وقت اجرا نمی‌شوند:
- `prisma migrate deploy` (اعمال migration های ساختاری روی دیتابیس)
- `prisma db seed` (ساخت داده اولیه/حساب‌های پیش‌فرض)

**نتیجه‌ی عملی:** هر تغییری که نیاز به migration دیتابیس یا seed داشته باشد، تا وقتی این محدودیت پلتفرم رفع نشود (یا یک Dockerfile اختصاصی اضافه شود، یا Liara یک «Release Command» رسمی ارائه بدهد)، باید **دستی** از طریق Shell سرور Liara اجرا شود. راهنمای گام‌به‌گام این کار در بخش [۳. راهنمای اجرای migration دستی](#۳-راهنمای-اجرای-migration-دستی) آمده.

نکته‌ی مهم‌تر: چون این محیط `standalone` است، خودِ **Prisma CLI هم روی سرور production نصب نیست** (فقط فایل‌های لازم برای اجرای خودِ سرور Next.js کپی می‌شوند، نه ابزارهای توسعه). یعنی حتی اگر به Shell وصل شوید، `npx prisma migrate deploy` هم کار نمی‌کند. برای تغییرات ساختاری باید یک اسکریپت Node مستقل نوشت که با پکیج `pg` مستقیم SQL اجرا کند.

این محدودیت **فقط** روی migration/seed اثر دارد. کاربردهای عادی سایت (ثبت مشتری با OTP، ثبت سفارش، ساخت شرکت/کارمند جدید از پنل ادمین، تعریف فرمول قیمت، تغییر وضعیت سفارش و…) هرکدام فقط `INSERT`/`UPDATE` معمولی روی جدول‌های موجودند (نه تغییر ساختار)، از طریق Server Action اجرا می‌شوند، و کاملاً خودکار و بدون هیچ مداخله‌ی دستی کار می‌کنند.

### ۲. حادثه‌ی migration دستی وزن (`weightKg` → `weightGrams`) — نیاز به تایید فوری

وقتی فیلد وزن سفارش از کیلوگرم به گرم تغییر کرد (بخش تاریخچه، ۲۰۲۶-۰۷-۳۰)، یک فایل migration رسمی در `prisma/migrations/20260730150457_convert_order_weight_to_grams/migration.sql` ساخته شد با این محتوا:

```sql
ALTER TABLE "orders"
  ALTER COLUMN "weightKg" TYPE INTEGER USING ROUND("weightKg" * 1000)::INTEGER;
ALTER TABLE "orders" RENAME COLUMN "weightKg" TO "weightGrams";
```

اما چون طبق بند ۱ بالا `scripts/start.sh` هیچ‌وقت روی سرور واقعی اجرا نشد، این migration **هیچ‌وقت از طریق `prisma migrate deploy` روی دیتابیس production اعمال نشد**. کاربر (نه دستیار) مستقیماً از طریق Shell سرور Liara و یک اسکریپت Node با پکیج `pg`، این دو دستور SQL را دستی روی دیتابیس واقعی اجرا کرد:

```sql
ALTER TABLE orders RENAME COLUMN "weightKg" TO "weightGrams";
UPDATE orders SET "weightGrams" = "weightGrams" * 1000;
```

**دو مشکل واقعی از این مغایرت به‌وجود آمده که باید تایید/رفع شوند:**

#### الف) عدم تطابق بین SQL دستی و migration رسمی — ریسک نوع ستون (Type)

migration رسمی، ستون را از `DECIMAL(6,2)` به `INTEGER` **تغییر نوع** هم می‌دهد (`ALTER COLUMN ... TYPE INTEGER`). SQL دستی‌ای که واقعاً اجرا شده **این تغییر نوع را ندارد** — فقط تغییر نام ستون و ضرب مقادیر در ۱۰۰۰ انجام شده. یعنی به احتمال زیاد **ستون `weightGrams` روی دیتابیس واقعی هنوز از نوع `DECIMAL(6,2)` است، نه `INTEGER`**، درحالی‌که `schema.prisma` می‌گوید `weightGrams Int?`.

**باید فوراً روی دیتابیس production تایید شود:**
```sql
SELECT column_name, data_type, numeric_precision, numeric_scale
FROM information_schema.columns
WHERE table_name = 'orders' AND column_name = 'weightGrams';
```
اگر `data_type` چیزی غیر از `integer` بود (مثلاً `numeric`)، باید یک ALTER اصلاحی دیگر اجرا شود:
```sql
ALTER TABLE orders ALTER COLUMN "weightGrams" TYPE INTEGER USING ROUND("weightGrams")::INTEGER;
```
(دقت: اینجا دیگر نباید دوباره در ۱۰۰۰ ضرب شود — مقادیر از قبل به گرم تبدیل شده‌اند، فقط باید نوع واقعی ستون هم با schema هماهنگ شود.)

#### ب) ریسک واقعی سرریز عددی (numeric overflow) حین اجرای `UPDATE`

`DECIMAL(6,2)` یعنی حداکثر ۶ رقم معنادار، ۲ رقم بعد از اعشار — یعنی بیشترین مقدار قابل‌ذخیره `9999.99` است. هر سفارشی که وزن اصلی‌اش (بر حسب کیلوگرم، قبل از تبدیل) **۱۰ کیلوگرم یا بیشتر** بوده، بعد از ضرب در ۱۰۰۰ به عددی مثل `10000.00` یا بزرگ‌تر می‌رسیده که **از ظرفیت `DECIMAL(6,2)` سرریز می‌کند** و PostgreSQL برای آن خطای `numeric field overflow` می‌دهد. چون `UPDATE` یک دستور SQL واحد است، اگر حتی یک ردیف چنین خطایی بدهد، **کل دستور UPDATE به‌صورت اتمیک rollback می‌شود** — یعنی اگر در آن لحظه حتی یک سفارش با وزن ≥۱۰ کیلوگرم در جدول وجود داشته (و seed خودِ پروژه تعرفه‌هایی تا ۵۰ کیلوگرم دارد، پس کاملاً محتمل است)، **کل `UPDATE` ممکن است اصلاً اجرا نشده باشد** درحالی‌که `RENAME COLUMN` (که یک دستور DDL جداگانه است) احتمالاً موفق بوده.

**باید فوراً روی دیتابیس production تایید شود:**
```sql
SELECT "trackingCode", "weightGrams" FROM orders WHERE "weightGrams" IS NOT NULL ORDER BY "weightGrams" DESC LIMIT 20;
```
مقادیر را چک کنید: آیا همه منطقی به‌نظر می‌رسند (چند صد تا چند ده‌هزار گرم)، یا مقادیری هست که بی‌معنی/خیلی کوچک/آسیب‌دیده به‌نظر می‌رسد (نشانه‌ی این‌که تبدیل درست انجام نشده)؟

#### ج) هماهنگ‌سازی تاریخچه‌ی migration با Prisma

چون این تغییر با SQL دستی انجام شده نه با `prisma migrate deploy`، جدول داخلی `_prisma_migrations` روی دیتابیس production **هیچ ردیفی برای `20260730150457_convert_order_weight_to_grams` ندارد**. اگر یک روز `prisma migrate deploy` روی این دیتابیس اجرا شود (چه به‌صورت خودکار بعد از رفع مشکل بند ۱، چه دستی)، Prisma این migration را «هنوز اجرا نشده» تشخیص می‌دهد و تلاش می‌کند دوباره اجرایش کند — که چون ستون `weightKg` دیگر وجود ندارد (به `weightGrams` تغییر نام داده)، با خطا (`column "weightKg" does not exist`) شکست می‌خورد و **کل فرآیند `migrate deploy` را متوقف می‌کند** (چون migration ها به‌ترتیب اجرا می‌شوند و با اولین خطا متوقف می‌شوند) — یعنی هیچ migration بعدی هم اعمال نمی‌شود.

**راه‌حل رسمی Prisma برای دقیقاً همین وضعیت** (تغییر دستی دیتابیس هماهنگ با یک migration موجود، بدون ثبت در تاریخچه) دستور `migrate resolve` است، **نه** ساخت یک migration جدید/خالی:
```sh
npx prisma migrate resolve --applied 20260730150457_convert_order_weight_to_grams
```
این دستور باید از محیطی اجرا شود که هم **Prisma CLI کامل** روی آن نصب باشد (نه سرور production standalone) و هم `DATABASE_URL` به همان دیتابیس production اشاره کند — مثلاً یک چک‌اوت کامل پروژه روی لپ‌تاپ توسعه‌دهنده با `DATABASE_URL` موقتاً ست‌شده روی مقدار production (با احتیاط کامل، و پاک‌کردن آن env var بلافاصله بعد از اجرا).

**ترتیب پیشنهادی برای رفع کامل این حادثه:**
1. تایید نوع واقعی ستون (بند الف) — اگر لازم بود، اصلاحش کنید.
2. بررسی صحت مقادیر (بند ب) — اگر داده خراب/ناقص بود، با یک اسکریپت اصلاحی دیگر ترمیمش کنید.
3. بعد از اطمینان از این‌که وضعیت واقعی دیتابیس دقیقاً با چیزی که migration رسمی توصیف می‌کند یکی است، `prisma migrate resolve --applied ...` را اجرا کنید تا تاریخچه هماهنگ شود.

### ۳. حساب‌های تست seed.ts — هیچ‌وقت واقعاً ساخته نشده‌اند

در `prisma/seed.ts` دو حساب تشخیصی idempotent تعریف شده:
- ادمین: `testadmin` / `Test@12345`
- شرکت: `testcompany` / `Test@12345` (متصل به یک «شرکت تست عیب‌یابی» که خودش هم در همان seed ساخته می‌شود)

اما چون طبق بند ۱ بالا `prisma db seed` هیچ‌وقت خودکار روی production اجرا نشده، **این دو حساب هیچ‌وقت واقعاً در دیتابیس production ساخته نشده‌اند**. تنها حساب‌هایی که واقعاً روی production کار می‌کنند، آن‌هایی هستند که در ابتدای پروژه (اولین باری که دیتابیس راه‌اندازی شد) به هر شکلی واقعاً seed شدند — یعنی `admin` (ادمین اصلی) و `azma-post` (حساب شرکت نمونه).

**این وضعیت باید یکی از این دو مسیر را طی کند:**
- **گزینه ۱ — حذف:** اگر قرار نیست به‌زودی از Shell سرور برای seed دستی استفاده شود، این دو حساب تست را از `seed.ts` حذف کنید — چون در حالت فعلی صرفاً گمراه‌کننده‌اند (هرکسی این فایل را بخواند فکر می‌کند این حساب‌ها روی production فعال‌اند).
- **گزینه ۲ — ساخت دستی:** اگر واقعاً به این حساب‌های تست نیاز دارید (مثلاً برای عیب‌یابی مشابه آنچه قبلاً برایش ساخته شدند)، باید دقیقاً مثل بند migration دستی بالا، یک اسکریپت Node با `pg` بنویسید که این دو ردیف را مستقیم در جدول‌های `employees` و `companies`/`company_accounts` درج کند (رمز باید با `bcryptjs` هش شود، دقیقاً مثل چیزی که `seed.ts` انجام می‌دهد — نمونه‌ی این الگو در بخش بعد آمده).

**نکته‌ی جانبی مرتبط و مهم:** حتی حساب اصلی `admin` هم اگر یک‌بار رمزش به هر روشی (دستی یا ...) عوض شده باشد، `seed.ts` **هیچ‌وقت آن را بازنویسی نمی‌کند** — چون منطق فایل این‌طور است: *«اگر کاربری با این username از قبل وجود دارد، هیچ کاری نکن»* (`if (!adminExists) { ...create... }`). یعنی اگر با رمز مستند‌شده در این فایل (`badro@admin1404`) نتوانستید وارد شوید، دلیلش لزوماً باگ نیست — ممکن است رمز واقعی دیتابیس با آنچه اینجا نوشته متفاوت باشد.

### ۴. ربات استعلام خودکار (`page_automation`) — موتور واقعی نوشته شد، ولی به هیچ شرکت واقعی وصل نیست

از ۲۰۲۶-۰۷-۳۱، `src/lib/pricing/page-automation-provider.ts` دیگر یک Interface خالی نیست — با **Playwright واقعی** پیاده‌سازی شده: صفحه‌ی شرکت را باز می‌کند، فیلدهای پیکربندی‌شده در `CompanyForm` (پنل ادمین) را پر می‌کند، چک‌باکس‌های لازم را تیک می‌زند، دکمه‌ی محاسبه را کلیک و قیمت را از selector نتیجه استخراج می‌کند. تمام مراحل تایم‌اوت‌دار هستند (ناوبری ۱۵ ثانیه، هر اکشن دیگر ۱۰ ثانیه) و هر خطا (selector پیدا نشد، صفحه لود نشد، قیمت قابل‌استخراج نبود) به‌جای کرش، `available: false` با `reason` مشخص برمی‌گرداند.

**⚠️ محدودیت مهم حین توسعه — چرا این موتور با شرکت واقعی «دکاپست» تست نشد:**

درخواست اولیه این بود که موتور مستقیماً روی `price.dekapost.ir` (شرکت دکاپست) پیاده و تست شود. اما **این sandbox (محیط اجرای Claude Code) دسترسی شبکه به `price.dekapost.ir` را در سطح پروکسی خروجی مسدود کرده** — این یک policy denial سازمانی است، نه محدودیتی از طرف خودِ سایت دکاپست. این مسئله با سه ابزار مختلف تایید شد: `curl` (خطای `403`)، `WebFetch` (خطای `403`)، و حتی یک مرورگر Chromium واقعی از طریق Playwright (خطای `ERR_TUNNEL_CONNECTION_FAILED`). یعنی **هیچ کدام از ابزارهای این نشست نمی‌توانستند صفحه‌ی واقعی را باز کنند** تا selectorهای دقیق ۹ فیلد و ۲ چک‌باکسی که کاربر توصیف کرده بود (شهر مبدا، شهر مقصد، وزن، ارزش کالا، نوع محتوا، طول، عرض، ارتفاع، «قبول از محل مشتری»، «مرسوله نیاز به بسته‌بندی ندارد») استخراج شود.

**به‌جای حدس‌زدن selectorهای غلط (که در production بی‌صدا شکست می‌خورد)**، مسیر زیر طی شد:
1. موتور به‌صورت کاملاً **عمومی و پیکربندی‌محور** طراحی شد (نه هاردکد برای دکاپست) — هر فیلد شناخته‌شده (`origin`, `destination`, `weight`, `declaredValue`, `length`, `width`, `height`, `contentType`) در `AutomationConfig.fieldSelectors` یک CSS selector می‌گیرد؛ `checkboxSelectors` آرایه‌ای از selector چک‌باکس‌هایی است که باید همیشه تیک بخورند؛ `extraStaticFields` برای فیلدهای خاص یک شرکت که در این لیست از پیش پیش‌بینی نشده (مثلاً یک فیلد نهم ناشناخته). این ساختار الان در `CompanyForm.tsx` (پنل ادمین) هم به‌طور کامل قابل ویرایش است — با UI افزودن/حذف ردیف برای چک‌باکس‌ها و فیلدهای اضافه (مثل الگوی از پیش موجود جدول پله‌ای وزن).
2. چون نه «نوع محتوا» و نه دو چک‌باکس در مدل سفارش بادرو معادل مستقیم ندارند (مشتری بادرو این‌ها را انتخاب نمی‌کند)، این‌ها به‌صورت **مقدار ثابتِ تعریف‌شده توسط ادمین** پر می‌شوند (`contentTypeValue`، و چک‌باکس‌ها همیشه تیک می‌خورند). فیلدهای دیگر (وزن، طول، عرض، ارتفاع، ارزش کالا) از داده‌ی واقعی سفارش/درخواست قیمت می‌آیند — که برای این کار، `PriceQuoteInput`/`QuoteRequest` (در `src/lib/pricing/types.ts` و `engine.ts`) با فیلدهای `lengthCm`, `widthCm`, `heightCm`, `declaredValue` تکمیل شد و در همه‌ی نقاط فراخوانی (`results/page.tsx`، `ParcelDetailsStep.tsx`، `actions/orders.ts`) هم به‌درستی پاس داده می‌شود — قبلاً این مقادیر در فرم سفارش جمع‌آوری می‌شدند ولی هیچ‌وقت به موتور قیمت‌گذاری نمی‌رسیدند.
3. برای اطمینان از درستی خودِ منطق (fill کردن select/input، تیک زدن چک‌باکس، کلیک دکمه، استخراج و parse قیمت فارسی، و مسیرهای خطا)، یک صفحه‌ی HTML آزمایشی محلی با ساختار مشابه (۸ فیلد + ۲ چک‌باکس + دکمه + نتیجه) ساخته و همان منطق دقیق کد production رویش اجرا شد — **نه روی دکاپست واقعی**. نتیجه: مسیر موفق قیمت را درست استخراج کرد (`۱۲۵,۰۰۰ تومان` → `125000`)، و هر دو مسیر خطای عمدی (selector نتیجه‌ی اشتباه، آدرس اشتباه) بدون کرش `available:false` با پیام خطای مشخص برگرداندند.
4. شرکت «دکاپست» **در دیتابیس ساخته نشده** (نه در seed، نه دستی) — چون بدون selector واقعی، ساخت یک ردیف `Company`/`PricingRule` با تنظیمات نصفه‌کاره صرفاً گمراه‌کننده بود.

**برای تکمیل واقعی اتصال دکاپست، این مراحل باقی مانده (نیاز به کسی با دسترسی شبکه‌ی واقعی به `price.dekapost.ir` دارد):**
1. صفحه‌ی `price.dekapost.ir` را در مرورگر معمولی باز کنید، با Inspect Element (کلیک راست → بازرسی) روی هر ۹ فیلد و ۲ چک‌باکس، selector دقیق (id یا attribute پایدار) هرکدام را پیدا کنید.
2. از `/admin/companies/new` یک شرکت جدید به نام «دکاپست» بسازید، «نحوه دریافت قیمت» را روی «ربات استعلام خودکار» بگذارید، و selectorهای واقعی را در فرم (که حالا همه‌ی ۹ فیلد + چک‌باکس‌ها + فیلدهای اضافه را پشتیبانی می‌کند) وارد کنید.
3. یک استعلام واقعی از `/results` تست کنید تا مطمئن شوید قیمت واقعی دکاپست برمی‌گردد.

**⚠️ ریسک جدید و مهم برای دیپلوی — باینری Chromium روی Liara از قبل نصب نیست:**

`playwright` به‌عنوان dependency اضافه شد، ولی خودِ باینری مرورگر Chromium (چند صد مگابایت) یک artifact جدا از npm package است و باید جداگانه دانلود/نصب شود (`npx playwright install --with-deps chromium`). این sandbox این باینری را از پیش نصب‌شده دارد (`PLAYWRIGHT_BROWSERS_PATH`)، **ولی محیط production روی Liara چنین چیزی ندارد** — دقیقاً همان کلاس مشکلی که در بخش ⚠️۱ برای `scripts/start.sh` توضیح داده شد: بدون یک Dockerfile اختصاصی یا Release Command که این نصب را قبل از اجرای سرور انجام دهد، `chromium.launch()` روی production با خطای «Executable doesn't exist» شکست می‌خورد و `page_automation` برای همه‌ی شرکت‌ها همیشه `available: false` برمی‌گرداند (بدون کرش کلی سایت، چون خطا مدیریت شده — ولی عملاً غیرفعال). این باید قبل از فعال کردن `page_automation` برای هر شرکت واقعی روی production حل شود. همچنین اجرای یک مرورگر headless داخل Server Action یک هزینه‌ی محسوس CPU/RAM/زمان (چند ثانیه به‌ازای هر استعلام) دارد که برای مقایسه‌ی هم‌زمان قیمت چند شرکت (مسیر `/results`) باید سنجیده شود.

---

## ۱. توضیح کامل پروژه

بادرو پلتفرم مقایسه قیمت و ثبت سفارش ارسال مرسوله در ایران است — هم پستی بین‌شهری، هم پیک درون‌شهری. مشتری مبدا/مقصد و مشخصات مرسوله را وارد می‌کند، قیمت چند شرکت طرف‌قرارداد را همزمان می‌بیند، و سفارش را بدون پرداخت آنلاین ثبت می‌کند (تسویه‌حساب مستقیم با شرکت در مبدا یا مقصد انجام می‌شود).

### استک فنی

| لایه | تکنولوژی |
|---|---|
| فرانت‌اند + بک‌اند | Next.js 16.2.10 (App Router + Turbopack)، TypeScript، React 19.2.4 — تک‌کدبیس فول‌استک |
| استایل | Tailwind CSS v4 |
| دیتابیس | PostgreSQL |
| ORM | Prisma 7.8 با `@prisma/adapter-pg` (driver adapter؛ `datasource.url` از `prisma.config.ts`/env خوانده می‌شود، نه از `schema.prisma`) |
| احراز هویت | JWT (`jose`) — سه کوکی جدا برای مشتری/کارمند/شرکت: `badro_customer_session`, `badro_staff_session`, `badro_company_session`. رمزهای عبور با `bcryptjs` هش می‌شوند (`src/lib/auth/password.ts`) |
| نقشه | Leaflet / OpenStreetMap، به‌همراه دکمه‌ی Geolocation مرورگر («موقعیت من») |
| نمودار | Recharts |
| آیکون | lucide-react |
| PWA | `manifest.ts` + service worker دستی (`public/sw.js`) — بدون رفتار مسیر‌محور خاص |
| SMS | لایه انتزاعی `SmsProvider` (`src/lib/sms/`) — پیاده‌سازی‌های موجود: `mock` و `kavenegar` (واقعی، جزئیات در بخش ۲) |
| میزبانی | **Liara**، بدون Dockerfile (کاملاً auto-detect-محور). محدودیت‌های مهمش در بخش ⚠️ بالا توضیح داده شده |

نکته: در Next.js 16 نام `middleware.ts` به `proxy.ts` تغییر کرده — فایل معادل پروژه: `src/proxy.ts`.

### ساختار کلی

```
src/
├── app/                       # صفحات (App Router)
│   ├── (public)/               # صفحه اصلی، نتایج، سفارش، رهگیری، درباره/تماس/قوانین
│   ├── admin/                   # پنل ادمین/کارمندان (route group (dashboard) + login)
│   ├── company/                  # پنل شرکت‌ها (route group (dashboard) + login + test)
│   └── panel/                     # پنل مشتری
├── actions/                    # Server Actions — تنها نقطه‌ی ورود عملیات نوشتنی (معادل لایه API)
│   ├── admin/                   # companies.ts، employees.ts، orders.ts، content.ts
│   ├── auth.ts                   # لاگین/خروج هر سه نقش
│   ├── company-orders.ts          # تغییر وضعیت سفارش از پنل شرکت
│   ├── company-profile.ts          # تغییر رمز عبور خودِ شرکت
│   ├── orders.ts, quote.ts, profile.ts, contact.ts, tracking.ts
├── lib/
│   ├── pricing/                  # موتور قیمت‌گذاری — جزئیات کامل در بخش ۲
│   ├── auth/                      # session (JWT هر نقش)، OTP، password (bcryptjs)، RBAC کارمندان
│   ├── sms/                        # لایه انتزاعی ارسال پیامک: provider.ts + kavenegar-provider.ts
│   └── prisma.ts                    # singleton کلاینت Prisma (با adapter-pg)
├── components/
│   ├── admin/                    # CompanyForm، EmployeeForm و فرم‌های ادمین
│   ├── company/                   # ChangePasswordForm پنل شرکت
│   ├── order/                      # AddressFields، MapPicker، OrderForm، ParcelRow
│   ├── home/                        # HomeServiceFlow (ویزارد صفحه اصلی)، ParcelDetailsStep
│   └── ...
└── proxy.ts                        # میان‌افزار احراز هویت مسیرها (جایگزین middleware.ts قدیمی)

prisma/
├── schema.prisma      # ~۱۵ مدل کامل (User, Employee, Company, CompanyAccount, Order, PricingRule, ...)
├── seed.ts             # داده اولیه idempotent — طبق بخش ⚠️۱، خودکار روی production اجرا نمی‌شود
└── migrations/         # طبق بخش ⚠️۱، خودکار روی production اعمال نمی‌شوند؛ نیاز به اجرای دستی دارند
```

معماری نوشتن داده فقط از طریق Server Actions است — هیچ Route Handler واقعی برای نوشتن وجود ندارد.

---

## ۲. وضعیت کامل فیچرها

### ✅ کاملاً تکمیل و فعال

| فیچر | جزئیات |
|---|---|
| سه پنل | مشتری (`/panel`، OTP)، ادمین/کارمندان (`/admin`، یوزرنیم/پسورد + RBAC جدول‌محور)، شرکت‌ها (`/company`، یوزرنیم/پسورد) |
| موتور قیمت‌گذاری `internal_formula` | فرمول ریاضی ساده + جدول پله‌ای؛ ورودی وزن بر حسب **گرم**، تعرفه‌های ادمین (`pricePerKg`, `weightTiers`) همچنان بر مبنای کیلوگرم — تبدیل داخل `internal-formula-provider.ts` انجام می‌شود |
| چرخه سفارش | ثبت → تغییر وضعیت (توسط شرکت/ادمین) → پیگیری با کد رهگیری → تاریخچه کامل وضعیت |
| مدیریت حساب کاربری شرکت‌ها | ادمین از `CompanyForm` می‌تواند یوزرنیم/رمز `CompanyAccount` هر شرکت را بسازد/ویرایش کند؛ خودِ شرکت هم از `/company/settings` می‌تواند رمزش را تغییر دهد |
| گزارش‌ها | گزارش کمیسیون ادمین (نمودار + CSV) + گزارش مالی هر شرکت |
| مدیریت محتوا | اسلایدر صفحه اصلی از `/admin/content` |
| فرم ثبت سفارش | آدرس فقط با هشدار لحظه‌ای برای حروف غیرفارسی (نه فیلتر بی‌صدا)، دکمه‌ی Geolocation «موقعیت من»، الزام واقعی انتخاب موقعیت روی نقشه (پیک: مبدا+مقصد اجباری، پستی: فقط مبدا اجباری)، اسکرول خودکار به بالا بین مراحل ویزارد |
| SMS واقعی (کاوه‌نگار) | `KavenegarSmsProvider` به REST API واقعی وصل می‌شود — ولی فقط با **خط رایگان/تست** امتحان شده؛ ارسال به شماره‌های دیگر با خطای ۵۰۱ مواجه می‌شود چون نیاز به خرید خط اختصاصی دارد |

### 🟡 نیمه‌کاره — فقط UI/ذخیره‌سازی تنظیمات آماده است، منطق اصلی هنوز نوشته نشده

| فیچر | چه چیزی آماده است | چه چیزی باقی مانده |
|---|---|---|
| `PricingSourceType.external_api` | در `CompanyForm` قابل انتخاب؛ دو فیلد «آدرس API»/«کلید API» در `Company.apiBaseUrl`/`Company.apiKey` ذخیره می‌شوند | `src/lib/pricing/external-api-provider.ts` هنوز فقط Interface خالی است — همیشه `available: false` برمی‌گرداند. باید فرمت واقعی request/response طراحی و پیاده‌سازی HTTP واقعی نوشته شود |
| `PricingSourceType.page_automation` | موتور واقعی با Playwright نوشته و روی یک صفحه‌ی HTML آزمایشی محلی تست شد (بخش ⚠️۴). `CompanyForm` از ۹ فیلد + چک‌باکس‌های نامحدود + فیلدهای اضافه‌ی سفارشی پشتیبانی می‌کند | **هیچ شرکت واقعی به آن وصل نیست.** به‌خاطر مسدود بودن دسترسی شبکه‌ی sandbox به `price.dekapost.ir` (بخش ⚠️۴)، selector واقعی دکاپست پیدا نشد و شرکت «دکاپست» در دیتابیس ساخته نشده. همچنین باینری Chromium روی محیط production لیارا از پیش نصب نیست — باید قبل از اولین استفاده‌ی واقعی حل شود (بخش ⚠️۴) |

### ❌ هنوز شروع نشده

| فیچر | توضیح |
|---|---|
| زون‌بندی قیمت (Zone-based pricing) | مدل داده فعلی بر پایه‌ی «فاصله»ی محاسبه‌شده از `CityDistanceIndex` است، نه «زون». برای پیاده‌سازی نیاز به مدل‌های جدید (`Zone`, نگاشت شهر→زون, جدول قیمت زون×زون) و migration واقعی دارد — جزئیات کامل طراحی در تاریخچه‌ی مکالمات قبلی بحث شده اما هیچ کدی نوشته نشده |
| اتصال واقعی به شرکت‌های حمل‌ونقل واقعی (مثل تیپاکس) | تمام شرکت‌های فعلی در `seed.ts` نمونه/ساختگی‌اند (آزما پست، پیک تیز پرواز، …) — نه نام واقعی شرکت‌ها ثبت شده، نه فرمول قیمت واقعی‌شان، نه API/ربات واقعی‌شان وصل شده |
| درگاه پرداخت آنلاین | عمداً خارج از محدوده — تسویه‌حساب مستقیم/حضوری با شرکت |
| SMS provider دوم (ملی‌پیامک) | فقط کامنت placeholder در `src/lib/sms/provider.ts`، هیچ کلاسی نوشته نشده |
| اپلیکیشن موبایل نیتیو | فقط PWA وجود دارد |

---

## ۳. راهنمای اجرای migration دستی

این راهنما دقیقاً همان روشی است که برای حادثه‌ی وزن (بخش ⚠️۲) استفاده شد. تا وقتی محدودیت `scripts/start.sh` (بخش ⚠️۱) رفع نشده، هر تغییر ساختاری روی دیتابیس production باید از همین مسیر انجام شود.

### مرحله ۱ — وصل شدن به Shell سرور از پنل Liara

1. وارد پنل Liara شوید و اپلیکیشن مربوط به بادرو را باز کنید.
2. به تب **«خط فرمان»** (Shell/Console) بروید — این یک ترمینال زنده داخل همان کانتینر در حال اجرا است.
3. مطمئن شوید در working directory درستی هستید (معمولاً همان جایی که `node_modules` و فایل‌های standalone قرار دارند — دستور `ls` بزنید تا مطمئن شوید `node_modules/pg` وجود دارد).

### مرحله ۲ — نوشتن اسکریپت اصلاحی با heredoc

از الگوی دقیق زیر استفاده کنید (`fix.js` فقط یک اسم نمونه است، هر اسمی می‌تواند باشد):

```sh
cat > fix.js << 'EOF'
const { Client } = require('pg');

async function main() {
  const client = new Client({ connectionString: process.env.DATABASE_URL });
  await client.connect();
  try {
    // SQL مورد نظر را اینجا بنویسید — مثال:
    await client.query('ALTER TABLE orders ALTER COLUMN "weightGrams" TYPE INTEGER USING ROUND("weightGrams")::INTEGER;');
    console.log('OK');
  } finally {
    await client.end();
  }
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
EOF
```

نکات مهم این مرحله:
- استفاده از `'EOF'` (با کوتیشن) نه `EOF` ساده، تا شل هیچ متغیری داخل متن اسکریپت را expand نکند.
- `process.env.DATABASE_URL` همان env var ای است که خودِ اپ هم برای اتصال به دیتابیس استفاده می‌کند — نیازی به وارد کردن دستی connection string (و لو رفتنش در تاریخچه‌ی ترمینال) نیست.
- پکیج `pg` نیازی به نصب جداگانه ندارد — چون `pg` از قبل یکی از dependency های پروژه است (برای `@prisma/adapter-pg`) و در build/standalone موجود است.

### مرحله ۳ — اجرا و بررسی نتیجه

```sh
node fix.js
```

اگر پیام خطا دید، **قبل از تلاش دوباره** دقیقاً بفهمید SQL چرا خطا داد (مثلاً سرریز عددی، ستون وجود ندارد، قفل روی جدول). هیچ‌وقت SQL تغییردهنده را کورکورانه دوباره اجرا نکنید.

برای تایید نتیجه، یک اسکریپت کوچک دیگر (یا همان الگو با یک `SELECT`) بنویسید و مقادیر واقعی را چاپ کنید — هیچ ابزار مانیتورینگ گرافیکی روی این محیط در دسترس نیست، پس تایید باید از طریق خروجی همین اسکریپت‌ها باشد.

### مرحله ۴ — پاک‌سازی

```sh
rm fix.js
```

فایل موقت را پاک کنید تا روی فایل‌سیستم کانتینر باقی نماند (هرچند با هر دیپلوی جدید فایل‌سیستم از نو ساخته می‌شود، پاک‌سازی دستی هم عادت خوبی است — خصوصاً وقتی اسکریپت شامل SQL حاوی جزئیات ساختار داخلی دیتابیس است).

### هشدارهای کلی

- **هیچ rollback خودکاری وجود ندارد.** قبل از هر `ALTER`/`UPDATE`، دقیقاً مطمئن شوید SQL درست است — ترجیحاً اول با یک `SELECT` مشابه، دامنه‌ی تأثیر را روی چند ردیف تست کنید.
- **این مسیر برای `prisma migrate deploy`/`prisma db seed` هم قابل استفاده نیست** — چون Prisma CLI روی این محیط نصب نیست. برای این دو دستور خاص، تنها راه واقعی، اجرا از یک محیط دیگر (لپ‌تاپ توسعه‌دهنده یا CI) با `DATABASE_URL` موقتاً روی مقدار production است، نه از همین Shell.
- بعد از هر migration دستی، **حتماً** یک migration رسمی متناظر در `prisma/migrations/` بسازید (اگر از قبل نساخته‌اید) تا `schema.prisma` و تاریخچه‌ی migration ها با واقعیتِ دیتابیس هماهنگ بمانند — و طبق بخش ⚠️۲(ج)، وقتی به محیطی با Prisma CLI دسترسی داشتید، `prisma migrate resolve --applied <نام migration>` را هم اجرا کنید تا بوکیپینگ Prisma هم هماهنگ شود.

---

## ۴. تاریخچه‌ی تغییرات

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — حذف مسیر تشخیصی `/api/debug`
مسیر `src/app/api/debug/route.ts` حذف شد. این یک روت تشخیصی موقت بود که بدون هیچ احراز هویتی شکل مقدار `DATABASE_URL` و نتیجه‌ی یک تلاش اتصال به دیتابیس را برمی‌گرداند — نشتی اطلاعاتی داخلی روی یک اندپوینت عمومی. ارجاعات این مسیر در `README.md` و `scripts/start.sh` هم پاک‌سازی شد.
**کامیت:** `f17f1d8`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — اتصال واقعی کاوه‌نگار برای ارسال OTP
کلاس واقعی `KavenegarSmsProvider` در `src/lib/sms/kavenegar-provider.ts` اضافه شد که به REST API کاوه‌نگار وصل می‌شود (`POST` به `sms/send.json`). در `src/lib/sms/provider.ts`، وقتی متغیر محیطی `SMS_PROVIDER=kavenegar` باشد، این کلاس به‌جای `MockSmsProvider` استفاده می‌شود. دو متغیر محیطی جدید لازم دارد: `KAVENEGAR_API_KEY` و `KAVENEGAR_SENDER` (به `.env.example` هم اضافه شدند، بدون مقدار واقعی).

**نکته مهم/محدودیت فعلی:** این اتصال فقط با **خط رایگان (تست) کاوه‌نگار** امتحان شده. ارسال به شماره‌های دیگر (غیر از شماره‌ی مالک اکانت/خط تست) با **خطای ۵۰۱** از سمت کاوه‌نگار مواجه می‌شود، چون خط رایگان محدودیت دارد و برای ارسال واقعی به مشتریان باید **خط اختصاصی خریداری شود**.

**کامیت:** `ae8a397`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — پنج بهبود در فرم ثبت سفارش

1. **وزن مرسوله از کیلوگرم به گرم:** فیلد ورودی وزن در ارسال پستی حالا گرم می‌گیرد. ستون `Order.weightKg` (Decimal) به `Order.weightGrams` (Int) تغییر نام و نوع داد — با migration که مقادیر سفارش‌های قبلی را با ضرب در ۱۰۰۰ به گرم تبدیل می‌کند تا داده‌ای از دست نرود. تعرفه‌های ادمین (`pricePerKg` در `PricingRule.formulaParams`، و `weightTiers`) همچنان بر مبنای کیلوگرم تعریف می‌شوند و دست‌نخورده ماندند؛ تبدیل گرم→کیلوگرم فقط داخل `internal-formula-provider.ts` و درست لحظه‌ی محاسبه قیمت انجام می‌شود. **⚠️ این migration هیچ‌وقت خودکار روی production اجرا نشد — جزئیات کامل حادثه و اقدام لازم در بخش ⚠️۲ همین سند.**
2. **محدودیت تایپ فارسی در آدرس:** در فیلدهای خیابان/کوچه/پلاک/طبقه/توضیحات، حروف انگلیسی حین تایپ به‌صورت خودکار حذف می‌شوند. علاوه‌براین، قبل از رفتن به مرحله بعد (یا ثبت نهایی سفارش)، اگر باز هم متن انگلیسی در آدرس تشخیص داده شود (مثلاً از طریق paste)، پیام «لطفا فارسی تایپ کنید» نمایش داده و ادامه مسدود می‌شود. (**این رفتار بعداً در تغییر بعدی اصلاح شد — به مورد بعدی نگاه کنید.**)
3. **دکمه «موقعیت من»:** زیر نقشه انتخاب آدرس (`MapPicker`) اضافه شد؛ با Geolocation مرورگر موقعیت فعلی کاربر را می‌گیرد، مارکر را روی همان نقطه می‌گذارد و نقشه را به آن نقطه پن می‌کند.
4. **رفع مشکل اسکرول بین مراحل:** ویزارد صفحه اصلی (`HomeServiceFlow`) هنگام رفتن به هر مرحله جدید (چه «بعدی» چه «بازگشت») صفحه را خودکار به بالا اسکرول می‌کند.
5. **الزام واقعی انتخاب موقعیت روی نقشه:** قبلاً `mapRequired` فقط یک هشدار بصری بود و مانع رفتن به مرحله بعد/ثبت سفارش نمی‌شد. حالا واقعاً اعمال می‌شود: در حالت **پیک موتوری**، انتخاب موقعیت روی نقشه برای هم مبدا هم مقصد اجباری است؛ در حالت **ارسال پستی**، فقط برای مبدا اجباری است و برای مقصد اختیاری می‌ماند (که پیش‌تر اصلاً نقشه‌ای برای مقصد نمایش داده نمی‌شد — حالا نمایش داده می‌شود، فقط اجباری نیست).

**کامیت:** `6a34d18`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — اصلاح رفتار محدودیت فارسی در فیلدهای آدرس

رفتار قبلی (حذف بی‌صدای حروف انگلیسی حین تایپ) گیج‌کننده بود — چون هیچ چیزی روی صفحه ظاهر نمی‌شد، کاربر فکر می‌کرد سایت مشکل دارد. جایگزین شد با:
- تایپ دیگر محدود نمی‌شود؛ کاربر می‌تواند هر متنی (فارسی یا انگلیسی) در فیلدهای خیابان/کوچه/پلاک/طبقه/توضیحات بنویسد.
- اگر متنِ داخل یکی از این فیلدها حرف انگلیسی داشته باشد، بلافاصله (همان لحظه تایپ) پیام خطای «لطفا فارسی تایپ کنید» درست چسبیده به لبه‌ی پایین همان فیلد نمایش داده می‌شود (با prop موجود `error` در کامپوننت `Input`، و به‌صورت دستی برای `textarea` توضیحات).
- تا وقتی حرف انگلیسی در آدرس باقی مانده، دکمه «بعدی» در ویزارد صفحه اصلی غیرفعال می‌ماند (مثل بقیه اعتبارسنجی‌های آن ویزارد)؛ در فرم نهایی ثبت سفارش (`OrderForm`) هم همچنان قبل از ارسال چک و مسدود می‌شود.

**کامیت:** `037eafb`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — حساب‌های تست تشخیصی + صفحه سلامت برای عیب‌یابی لاگین

دو مشکل گزارش شد: (۱) `/admin/login` باز می‌شود ولی با رمز درست هم «رمز اشتباه است» می‌دهد، (۲) `/company/login` روی چند دستگاه/مرورگر مختلف اصلاً باز نمی‌شود (نه یک مشکل کش محلی). بررسی کد نشون داد هیچ تفاوت ساختاری بین مسیر `admin/login` (که کار می‌کنه) و `company/login` (که کار نمی‌کنه) نیست — زنجیره‌ی import، `proxy.ts`، `session.ts`، کامپوننت‌های فرم، همه هم‌شکل و بدون فراخوانی دیتابیسی روی خود صفحات لاگین‌اند؛ چک کاراکترهای نامرئی یونیکد و تطابق حروف بزرگ/کوچک نام فایل‌ها هم چیزی پیدا نکرد. برای عیب‌یابی بیشتر (که نیاز به تست روی دیتابیس/سرور واقعی داشت):

- دو حساب تست تشخیصی به `prisma/seed.ts` اضافه شد (ایده‌مپوتنت، جدا از حساب‌های اصلی `admin`/`azma-post`): `testadmin`/`Test@12345` و `testcompany`/`Test@12345`.
- صفحه‌ی سلامت `GET /company/test` (`src/app/company/test/page.tsx`) اضافه شد — بدون هیچ import اضافه یا فراخوانی دیتابیس، فقط یک متن ساده. در `src/proxy.ts` هم از احراز هویت معاف شد.

**نتیجه‌ی تست‌ها (پیگیری بعدی):** `/company/test` باز شد ✅ (یعنی کل route group `/company` سالم است)، ولی `/company/login` همچنان کاملاً لود نمی‌شود ❌ — و حتی با `testadmin` تازه‌ساز هم `/admin/login` پیام «رمز اشتباه است» می‌دهد. **⚠️ نکته‌ی مهم: حساب‌های تست هیچ‌وقت واقعاً روی production ساخته نشدند چون seed خودکار اجرا نمی‌شود — جزئیات کامل در بخش ⚠️۳ همین سند.** علت اصلی مشکل `/company/login` هنوز به‌طور قطعی مشخص نشده؛ سرنخ‌های بعدی («multirun: one or more of the provided commands ended abnormally» در لاگ Liara) نشان می‌دهد این مشکل به لایه‌ی زیرساخت/پلتفرم Liara مربوط است، نه کد این ریپو (`multirun` هیچ‌جای کد پروژه استفاده نشده — باید بخشی از buildpack خودِ Liara باشد).

هر دو مورد (حساب‌های تست و صفحه‌ی `/company/test`) **تشخیصی‌اند** — بعد از رفع قطعی مشکل باید حذف بشن (دقیقاً مثل الگویی که قبلاً برای `src/app/api/debug` رعایت شد).

**کامیت:** `2ea6fe4`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — تکمیل مدیریت حساب کاربری شرکت‌ها (CompanyAccount)

قبل از این تغییر، مدل `CompanyAccount` در دیتابیس بود ولی **هیچ رابط کاربری‌ای برای ساخت/ویرایش آن وجود نداشت** — تنها راه ساخت یک حساب ورود پنل شرکت، دستکاری مستقیم دیتابیس (مثل `prisma/seed.ts`) بود؛ یعنی اگر ادمین از `/admin/companies/new` یک شرکت واقعی جدید می‌ساخت، آن شرکت اصلاً نمی‌توانست وارد پنل خودش شود. با همان الگوی از قبل پیاده‌شده برای `Employee` (فرم + اکشن با `hashPassword`)، این قابلیت کامل شد:

1. **`CompanyForm.tsx` / `saveCompanyAction`:** دو فیلد «نام کاربری» و «رمز عبور» به فرم ثبت/ویرایش شرکت اضافه شد.
   - ساخت شرکت جدید، یا ویرایش شرکتی که هنوز `CompanyAccount` ندارد (مثلاً شرکت‌های قدیمی‌تر از این تغییر) → رمز عبور اجباری (حداقل ۴ کاراکتر) و یک `CompanyAccount` جدید ساخته می‌شود.
   - ویرایش شرکتی که از قبل حساب دارد → رمز عبور اختیاری؛ اگر خالی بماند، رمز قبلی دست‌نخورده می‌ماند (دقیقاً مثل رفتار فرم کارمند).
   - یکتا بودن نام کاربری هم چک می‌شود (پیام خطای مشخص اگر تکراری باشد).
2. **صفحه‌ی جدید «تنظیمات حساب» در پنل شرکت** (`/company/settings`، لینک جدید در نویگیشن پنل شرکت): فرم استاندارد تغییر رمز عبور (رمز فعلی برای تایید + رمز جدید + تکرار رمز جدید) از طریق اکشن جدید `changeCompanyPasswordAction` (`src/actions/company-profile.ts`).

هیچ migration ای لازم نبود — مدل `CompanyAccount` از قبل در schema وجود داشت، فقط رابط کاربری/منطق نوشتن کم بود.

**کامیت:** `b1fbbc2`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — فعال‌سازی گزینه‌های API و ربات استعلام در فرم شرکت

دراپ‌داون «نحوه دریافت قیمت» در `CompanyForm.tsx` از قبل مقادیر `external_api` و `page_automation` را در enum داشت ولی برچسبشان «(به‌زودی)» بود و انتخابشان هیچ فیلد اضافه‌ای نشان نمی‌داد. حالا:

- **گزینه‌ی «API شرکت»:** با انتخابش، دو فیلد شرطی ظاهر می‌شود — «آدرس API» و «کلید API» (`type="password"` چون حساس است) — که مستقیماً در ستون‌های موجود `Company.apiBaseUrl` و `Company.apiKey` ذخیره می‌شوند.
- **گزینه‌ی «ربات استعلام خودکار» (`page_automation`):** با انتخابش، فیلدهای شرطی زیر ظاهر می‌شوند: آدرس صفحه‌ی استعلام قیمت شرکت، و Selector فرم آن صفحه (فیلد مبدا/مقصد/وزن، دکمه ثبت/محاسبه، محل نمایش نتیجه). این‌ها به‌صورت JSON با ساختار `{ url, fieldSelectors: {origin, destination, weight}, submitSelector, resultSelector }` در ستون موجود `PricingRule.formulaParams` ذخیره می‌شوند (یک ردیف `PricingRule` جدید با `sourceType: "page_automation"`، جدا از ردیف `internal_formula`).

**نکته‌ی مهم:** این تغییر فقط UI و ذخیره‌سازی تنظیمات است. **اجرای واقعی ربات (باز کردن صفحه‌ی شرکت با Playwright/Puppeteer، پرکردن فرم طبق این Selector ها، و خواندن قیمت خروجی) هنوز پیاده‌سازی نشده** — `src/lib/pricing/page-automation-provider.ts` هنوز فقط یک Interface خالی است که همیشه «در دسترس نیست» برمی‌گرداند. همین‌طور `src/lib/pricing/external-api-provider.ts` برای گزینه‌ی API هنوز فراخوانی HTTP واقعی ندارد. این‌ها فاز بعدی هستند؛ همین الان فقط تنظیمات ذخیره می‌شوند تا وقتی آن فاز پیاده شد، داده‌اش از قبل آماده باشد.

بدون migration — همه‌ی ستون‌های لازم (`Company.apiBaseUrl`, `Company.apiKey`, `PricingRule.formulaParams`) از قبل در schema بودند.

**کامیت:** `b5244f7`

### ۱۴۰۵/۰۵/۰۸ (۲۰۲۶-۰۷-۳۰) — بازنویسی کامل این سند

مستندات به‌طور کامل بازبینی و بازسازماندهی شد تا هر مهندس یا نشست جدید Claude Code بدون خواندن کل تاریخچه‌ی مکالمات، سریع context بگیرد. اضافه شد:
- بخش ⚠️۲: تحلیل کامل حادثه‌ی migration دستی وزن — شامل کشف این‌که SQL دستی اجراشده با migration رسمی یکی نیست (بدون تغییر نوع ستون)، ریسک واقعی سرریز عددی حین `UPDATE` برای سفارش‌های با وزن ≥۱۰ کیلوگرم، و مراحل رفع (تایید نوع ستون، تایید صحت داده، `prisma migrate resolve`).
- بخش ⚠️۳: روشن‌سازی این‌که حساب‌های تست (`testadmin`/`testcompany`) هیچ‌وقت واقعاً روی production ساخته نشدند.
- بخش ۳: راهنمای گام‌به‌گام قابل‌استفاده‌ی مجدد برای اجرای migration دستی از طریق Shell پنل Liara.
- بخش ۲ به‌طور کامل بازسازی شد تا وضعیت هر فیچر (تکمیل/نیمه‌کاره/شروع‌نشده) با جزئیات دقیق مشخص باشد.

### ۱۴۰۵/۰۵/۰۹ (۲۰۲۶-۰۷-۳۱) — پیاده‌سازی واقعی موتور ربات استعلام خودکار (`page_automation`) با Playwright

`playwright` به‌عنوان dependency اضافه شد و `src/lib/pricing/page-automation-provider.ts` از یک Interface خالی به یک موتور واقعی و عمومی تبدیل شد: صفحه‌ی شرکت را باز می‌کند، فیلدها/چک‌باکس‌ها را طبق تنظیمات ذخیره‌شده در `PricingRule.formulaParams` پر می‌کند، دکمه‌ی محاسبه را کلیک و قیمت را استخراج می‌کند — با مدیریت کامل خطا (تایم‌اوت ۱۰-۱۵ ثانیه، هر شکستی `available:false` برمی‌گرداند، نه کرش).

`AutomationConfig` (در `company-form-types.ts`) و UI مربوطه در `CompanyForm.tsx` بازطراحی شد تا عمومی باشد: ۸ فیلد شناخته‌شده (مبدا، مقصد، وزن، ارزش کالا، طول، عرض، ارتفاع، نوع محتوا)، لیست نامحدود چک‌باکس (برای «قبول از محل مشتری»/«نیاز به بسته‌بندی ندارد» و مشابه)، و لیست نامحدود «فیلد اضافه با مقدار ثابت» برای فیلدهای غیرمنتظره‌ی هر شرکت. `PriceQuoteInput`/`QuoteRequest` هم با `lengthCm`, `widthCm`, `heightCm`, `declaredValue` تکمیل شد تا این داده‌ها (که در فرم سفارش از قبل جمع‌آوری می‌شدند ولی به موتور قیمت‌گذاری نمی‌رسیدند) در `results/page.tsx`، `ParcelDetailsStep.tsx` و `actions/orders.ts` هم واقعاً پاس داده شوند.

**⚠️ کار روی شرکت «دکاپست» (`price.dekapost.ir`) که هدف اصلی این درخواست بود، ناتمام ماند** — چون sandbox این نشست دسترسی شبکه به آن دامنه را در سطح پروکسی خروجی مسدود می‌کند (برای هر ابزاری، حتی یک مرورگر Playwright واقعی)، امکان باز کردن صفحه، استخراج selectorهای واقعی، یا اجرای یک تست واقعی وجود نداشت. به‌جای این، منطق موتور روی یک صفحه‌ی HTML آزمایشی محلی هم‌ساختار تست و تایید شد (مسیر موفق + دو مسیر خطا). جزئیات کامل محدودیت، دلایل طراحی، و مراحل باقی‌مانده برای تکمیل واقعی دکاپست (شامل ریسک جدید نصب‌نبودن Chromium روی production) در بخش ⚠️۴ همین سند آمده.

بدون migration — `PricingRule.formulaParams` (`Json?`) از قبل در schema بود، فقط شکل داده‌ی داخلش تغییر کرد.

**کامیت:** (بعد از این commit ثبت می‌شود)

---

*این فایل به‌صورت خودکار توسط دستیار پس از هر تغییر مهم به‌روزرسانی می‌شود.*
